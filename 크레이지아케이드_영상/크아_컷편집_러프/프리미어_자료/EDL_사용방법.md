# 사용 방법

1. 프리미어 프로에서 `파일 > 가져오기`를 누른다.
2. `CRA_PLAY_ROUGHCUT_V1.edl`을 선택한다.
3. 오프라인/연결 창이 뜨면 원본 영상 `2026-06-26 19-42-34.mkv`를 선택한다.
4. 생성된 시퀀스에서 오디오가 꺼져 있으면 트랙 왼쪽의 `M` 또는 비활성 상태만 풀어준다.

원본 영상은 복사하거나 이름을 바꾸지 않았다. 이 파일은 원본을 참조해서 컷 순서만 만드는 용도다.
