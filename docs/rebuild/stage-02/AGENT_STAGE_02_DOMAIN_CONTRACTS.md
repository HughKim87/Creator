# 단계 02 작업 지시서: 도메인 계약과 상태 전이

## 1. 단계 목적

저장 방식이나 편집 도구를 구현하기 전에 프로젝트의 유효한 상태, 식별자, 승인,
산출물 계보를 순수 도메인 계약으로 고정한다. 이 계약은 이후 SQLite와 편집 어댑터가
따라야 할 기준이며 특정 파일 시스템이나 DB에 의존하지 않는다.

## 2. 시작 게이트

- 단계 01 전체 검사가 clean-clone에서 통과했다.
- 거짓 성공 가능성이 0건이다.
- 사용자가 단계 02 설계·구현을 승인했다.
- 단계 00의 계약·상태 관련 `PRESERVE`와 `REDESIGN` 목록을 읽었다.

## 3. 입력 자료

- `backup/docs/WORKFLOW_CONTRACT.json`
- 구체계의 source identity, generation lock, approval 관련 테스트
- 단계 00 `MIGRATION_CATALOG.md`
- 단계 01 공개 CLI·검증 계약

구체계의 저장 형식과 여러 상태 파일을 그대로 복제하지 않고 의미만 추출한다.

## 4. 역할과 소유권

### 도메인 구현 에이전트

- `src/video_workflow/domain/**`
- `tests/unit/domain/**`

### 계약 QA 에이전트

- `tests/contract/domain/**`
- `tests/property/**`

### 레드팀 에이전트

- 기본 읽기 전용
- 승인 우회, 불법 상태 전이, ID 재사용, 세대 덮어쓰기 시도

총괄 에이전트는 타입 이름과 공개 함수 시그니처를 먼저 동결한다. QA는 구현 파일을
고치지 않는다.

## 5. 구현할 핵심 타입

최소 타입:

- `ProjectId`
- `SourceId`
- `GenerationId`
- `ArtifactId`
- `EventId`
- `Phase`
- `LifecycleStatus`
- `ActorKind`
- `ApprovalType`
- `ApprovalDecision`
- `ArtifactRole`
- `SourceFingerprint`
- `WorkflowEvent`
- `ArtifactRecord`
- `ApprovalRecord`
- `FailureRecord`

문자열 alias만 만들지 말고 생성 규칙과 검증을 제공한다. 외부 입력에는 엄격 검증을
사용하고 잘못된 숫자·문자열을 암묵적으로 변환하지 않는다.

## 6. 단계와 수명주기의 이축 모델

정확한 명칭은 사용자 의미 승인 후 확정하되, 최소 의미는 다음을 포함한다.

```text
Phase:
intake → planning → editing → technical_validation → human_approval → delivery

LifecycleStatus:
active | waiting_user | blocked_external | failed | completed | cancelled
```

`Phase`는 작업 위치이고 `LifecycleStatus`는 진행 가능성이다. `waiting_user`,
`blocked_external`, `failed`로 바뀌어도 현재 phase는 보존한다. 재개는 보존된 phase로
돌아가며, 완료는 delivery phase와 필수 승인·검증을 모두 요구한다. 실행 단위 중단은
세대 또는 run의 `aborted` 결과로 기록하되 프로젝트 lifecycle과 혼합하지 않는다.

전역 단일 활성 작업 상태는 만들지 않는다. 각 프로젝트와 세대가 자신의 상태를 가진다.

## 7. 반드시 유지할 불변조건

### 원본과 식별자

- 한 원본 지문은 하나의 `source_id`에 대응한다.
- 같은 `source_id`에 다른 지문을 연결할 수 없다.
- 등록 후 원본 지문이 달라지면 다음 단계로 진행할 수 없다.
- 입력 원본과 산출물 역할을 동시에 가질 수 없다.

### 세대와 산출물

- 완료·승인된 이전 세대를 덮어쓰지 않는다.
- 모든 산출물은 `project_id`, `generation_id`, `source_id`, 부모 계보를 가진다.
- 실패한 세대의 산출물을 현재 산출물로 승격하지 않는다.
- 파일 존재만으로 산출물 등록을 대신하지 않는다.

### 승인과 단계 전이

- 생성이 필요한 단계는 명시된 사전 승인을 요구한다.
- 기술 검증과 사람 A/V 승인은 별도 타입과 이벤트다.
- 승인 이벤트는 승인 대상의 해시 또는 세대 기준선을 포함한다.
- 대상이 변경되면 이전 승인을 재사용할 수 없다.
- `waiting_user`를 성공이나 완료로 취급하지 않는다.
- 승인 actor는 `ActorKind.HUMAN`과 검증된 입력 경로·세션·결정 시각 provenance를 가진다.
  에이전트나 자동화 actor가 문자열만 바꾸어 사람 승인을 가장할 수 없다.

### 실패

- 실패 결과를 삭제하지 않고 고유 실패 지문과 증거를 기록한다.
- 같은 명령을 변경 없이 반복한 것은 별도 해결 시도로 세지 않는다.
- 오류 예외를 경고 성공으로 바꾸지 않는다.

## 8. 세부 작업

### 작업 02-1: 구체계 계약 대응표

구체계의 각 계약 필드를 다음으로 매핑한다.

- 유지되는 의미
- 이름만 변경되는 의미
- 저장 계층으로 이동할 의미
- 폐기되는 의미
- 새로 필요한 의미

모호한 항목을 구현자가 임의 결정하지 않고 사용자 승인 항목으로 올린다.

### 작업 02-2: 순수 전이 함수

전이 함수는 현재 상태, 요청 행동, 승인·검증 사실과 외부에서 공급된 `command_id`,
`event_id`, UTC `occurred_at`, actor provenance를 입력받아 다음 중 하나만 반환한다.

- 유효한 새 상태와 생성할 도메인 이벤트
- 구조화된 거부 사유

파일을 읽거나 DB에 쓰거나 외부 명령을 실행하지 않는다. 같은 입력에는 항상 같은
결과를 반환한다. UUID와 현재 시각은 순수 함수 내부에서 생성하지 않으며 별도 응용
계층이 공급한다.

### 작업 02-3: 계약 직렬화

- 향후 DB와 JSON 출력이 사용할 스키마 버전을 포함한다.
- 시간은 UTC 기준으로 직렬화한다.
- ID, enum, 해시 길이와 형식을 엄격히 검증한다.
- 알 수 없는 필드와 버전의 처리 정책을 명시한다.
- 알 수 없는 미래 스키마 버전은 fail-closed한다. 알 수 없는 필드는 버전별로 거부 또는
  보존 정책을 고정하며 구현자가 조용히 버리지 않는다.
- 로그에 비밀값이나 원본 콘텐츠 전문을 넣지 않는다.

### 작업 02-4: 속성 테스트

상태 전이 시퀀스를 자동 생성하여 다음을 검증한다.

- 허용 목록 밖 전이가 절대 성공하지 않음
- 최종 상태에서 금지된 재진입이 없음
- 승인 없이 생성 상태에 도달할 수 없음
- 원본 지문 변경 후 진행할 수 없음
- 모든 성공 전이에 이전·다음 상태와 이벤트 ID가 존재
- 같은 요청 ID 재사용이 멱등하게 처리되거나 명시적으로 거부됨
- `waiting_user`, `blocked_external`, `failed` 진입과 재개가 phase를 잃지 않음
- 동일 입력과 동일 event/time 입력은 바이트 수준에서 같은 결과를 생성함

## 9. 필수 예제·부정 테스트

- 같은 지문에 다른 `source_id`
- 같은 `source_id`에 다른 지문
- 승인 없는 `generating` 진입
- 다른 세대에 대한 승인 재사용
- 승인 후 입력 해시 변경
- 완료 세대 덮어쓰기
- `waiting_user`를 완료로 변환
- 실패 증거 없이 `failed` 진입
- 존재하지 않는 부모 산출물
- 순환 산출물 계보
- 잘못된 해시 길이와 잘못된 ID
- 문자열 숫자 등 암묵적 타입 변환
- 같은 전이 명령의 중복 전송
- 동일 요청 ID와 다른 action 또는 payload 조합
- 알 수 없는 스키마 버전과 정책 밖 알 수 없는 필드
- 에이전트 actor가 `ActorKind.HUMAN` 승인 provenance를 위조하는 시도
- `waiting_user`와 `failed` 진입·재개 중 phase 손실
- 내부 시계나 UUID 사용으로 같은 입력 결과가 달라지는 시도

## 10. 사용자 의미 승인 게이트

사용자에게 코드가 아니라 다음 의미를 제시하고 승인을 받는다.

1. 전체 단계 순서
2. 반드시 사람이 승인해야 하는 지점
3. `blocked_external`, `waiting_user`, `failed`, 실행 단위 `aborted`의 차이
4. 승인 대상이 바뀌었을 때 승인 무효화 조건
5. 완료 후 재개 또는 새 세대 생성 정책
6. 실패 세대와 증거 보존 기간

이 승인이 없으면 단계 03 저장소 구현을 시작하지 않는다.

## 11. 완료 기준

- 도메인 모듈이 파일 시스템·SQLite·FFmpeg를 import하지 않음
- 모든 식별자와 전이가 엄격 검증됨
- 승인 없는 생성 경로 0건
- 원본 지문 변경 우회 0건
- 속성 테스트와 부정 테스트 통과
- 구체계 계약 대응표 완성
- 레드팀이 불법 전이 경로를 찾지 못함
- 사용자 의미 승인 완료

## 12. 중단 조건

- 테스트를 통과시키기 위해 승인 조건을 약화해야 함
- 상태 의미를 DB 스키마에 맞춰 억지로 변경함
- 구체계 보존 대상의 의미가 사라짐
- 두 종류의 상태 원본을 전제로 해야 함
- 사용자 승인 의미가 불명확함

## 13. 롤백

단계 01 마지막 통과 지점으로 돌아가고 단계 02의 도메인·테스트 파일만 제거하거나
되돌린다. 저장 DB가 아직 없으므로 데이터 마이그레이션 롤백은 발생하지 않아야 한다.
