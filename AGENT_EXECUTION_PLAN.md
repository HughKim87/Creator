# 멀티에이전트 프로젝트 재구축 실행 계획

상태: 실행 전 계획

작성 기준일: 2026-07-16

이 문서는 `backup/`에 보존된 구체계에서 검증된 기능만 신규 구조로 이식하기 위한
최상위 실행 계획이다. 이 문서와 단계별 지시서는 작업 범위와 승인 조건을 정의할 뿐,
각 단계의 구현을 자동으로 승인하지 않는다. 쓰기, 외부 설치, Git 태그·커밋·푸시는
해당 단계 착수 전에 사용자의 명시적 승인을 받아야 한다.

기존 `PROJECT_STRUCTURE_RESEARCH.md`가 말하는 “0단계 최소 골격”은 이 계획의 Stage 01에
해당한다. 이 계획의 Stage 00은 그 골격을 만들기 전에 구체계 기준선을 동결하기 위해
추가한 안전 단계다. 기존 리서치와 핸드오프의 실측 사실은 보존하되, 이후 실행 순서와
게이트가 충돌하면 이 계획과 해당 단계 지시서를 따른다.

## 1. 최종 목표

1. 실패가 성공으로 보고되지 않는 fail-closed 실행·검증 체계를 만든다.
2. 프레임워크 코드와 영상 작업 데이터를 물리적으로 분리한다.
3. 프로젝트별 상태를 하나의 트랜잭션 저장소에서 관리한다.
4. 구체계의 정상 편집·동기화·승인 기능을 수직 단위로 선별 이식한다.
5. 실제 영상 파일럿과 복구 시험을 통과한 뒤에만 신규 구조로 전환한다.
6. Graphify와 Obsidian은 기반 안정화 이후 측정 가능한 파일럿으로만 도입한다.

## 2. 모든 에이전트가 지켜야 할 불변 조건

- `backup/`은 읽기 전용이다. 그 안의 파일을 수정·삭제·이름 변경·정리하지 않는다.
- `inputs/` 또는 외부 작업 공간의 원본 미디어를 덮어쓰지 않는다.
- 기존 정상 기능은 특성 테스트와 비교 기준 없이 재작성하지 않는다.
- 검사 도구, Python, FFmpeg, Git 상태가 없거나 해석되지 않으면 성공으로 건너뛰지 않는다.
- 상태 원본은 하나만 둔다. 핸드오프와 상태 문서는 원본 상태에서 생성한다.
- 로컬 검사와 CI는 같은 진입 명령을 사용한다.
- 테스트 통과와 사람의 영상·음성 품질 승인을 같은 것으로 취급하지 않는다.
- 유료 서비스, 외부 업로드, 네트워크 전송, 커뮤니티 플러그인은 사전 승인을 받는다.
- 예상하지 못한 기존 변경을 발견하면 보존하고 작업을 중단한 뒤 보고한다.
- 롤백 전에 실패 DB·이벤트·산출물·매니페스트를 증거로 동결한다. 복구를 이유로 실패
  증거를 제자리에서 덮어쓰지 않는다.
- 테스트 실패 상태에서 다음 단계로 넘어가지 않는다.
- 에이전트는 사용자 승인 없이 커밋·푸시·태그 생성·브랜치 보호 설정을 하지 않는다.

## 3. 필수 문서 읽기 순서

단계 담당 에이전트는 작업 시작 전에 다음 순서로 읽는다.

1. 현재 루트의 `PROJECT_RULES.md`가 존재하면 전체 내용
2. `AGENT_EXECUTION_PLAN.md`
3. 자신이 담당한 `AGENT_STAGE_XX_*.md`
4. 직전 단계의 `reports/rebuild/stage-XX/STAGE_REPORT.md`
5. 현재 `SESSION_HANDOFF.md`
6. 조정 에이전트와 Stage 00~01 담당자는 `PROJECT_STRUCTURE_RESEARCH.md`
7. 이식 대상이 있을 때만 `backup/`의 해당 코드·테스트·계약

관련 없는 구체계 문서를 모두 읽어 컨텍스트를 팽창시키지 않는다.

## 4. 역할 모델

### 조정 에이전트

- 단계 순서, 파일 소유권, 사용자 승인 상태를 관리한다.
- 공통 루트 파일의 최종 통합만 담당한다.
- 구현 에이전트의 작업이 완료되기 전에 같은 파일을 수정하지 않는다.

### 구현 에이전트

- 단계 지시서의 허용 쓰기 범위만 변경한다.
- 먼저 실패하는 테스트 또는 특성 테스트를 만들고 구현한다.
- 검증 결과와 변경 파일 목록을 단계 보고서 초안으로 남긴다.

### 검증 에이전트

- 구현 파일을 수정하지 않고 검증 명령, diff, 생성 증거를 점검한다.
- 성공 경로뿐 아니라 결손 환경, 잘못된 전이, 중단·복구 경로를 실행한다.
- 문제가 있으면 단계를 실패로 판정하고 구현 에이전트에게 돌려보낸다.

### 레드팀 에이전트

- 종료 코드 은폐, 입력 변조, 승인 우회, 낡은 상태 뷰, 경로·인코딩 오류를 시도한다.
- 발견한 결함마다 재현 절차와 기대 결과를 기록한다.
- 단계 1, 2, 3, 5, 6에서는 반드시 별도 검증 역할을 둔다.

### 사용자

- 단계 착수, 비용 발생, 사람 A/V 승인, 전환, 폐기를 승인한다.
- 자동 검사가 대신할 수 없는 콘텐츠 품질의 최종 승인자다.

## 5. 병렬 작업과 파일 소유권 규칙

초기 단계는 공통 루트 파일이 많으므로 구현을 순차 진행한다. 병렬 작업은 다음 경우만
허용한다.

- 한 에이전트가 구현하는 동안 다른 에이전트가 읽기 전용으로 테스트 설계·감사를 수행
- 서로 다른 테스트 디렉터리 또는 보고서 파일처럼 쓰기 범위가 완전히 분리된 경우
- 조정 에이전트가 작업 지시서에 구체적인 파일 목록을 부여한 경우

금지 사항:

- 둘 이상의 에이전트가 `pyproject.toml`, `uv.lock`, CI, 훅, DB 스키마를 동시에 수정
- 둘 이상의 에이전트가 같은 단계 보고서를 동시에 수정
- `backup/`에서 실행 중인 구체계 파일을 신규 에이전트가 수정
- 파일 소유권 합의 없이 대량 이동·이름 변경·자동 포맷 실행

Git worktree를 사용할 경우에도 공용 외부 미디어에 대한 쓰기는 분리한다. 에이전트별
산출물 경로를 사용하고, 상태 DB는 프로젝트별 단일 writer 규칙을 지킨다.

## 6. 단계 흐름

| 단계 | 문서 | 핵심 결과 | 다음 단계 승인 조건 |
|---|---|---|---|
| 00 | `AGENT_STAGE_00_BASELINE_FREEZE.md` | 구체계 기준선·보존 목록·골든 기준 | `backup/` 변경 0건, 기준선 증거 완성 |
| 01 | `AGENT_STAGE_01_TRUSTED_FOUNDATION.md` | 잠금 환경·단일 CLI·fail-closed 훅/CI | 거짓 성공 0건, clean-clone 검사 통과 |
| 02 | `AGENT_STAGE_02_DOMAIN_CONTRACTS.md` | 순수 도메인 계약·상태 의미 | 불법 전이·승인 우회 0건, 사용자 의미 승인 |
| 03 | `AGENT_STAGE_03_SINGLE_STATE_MODEL.md` | SQLite 단일 상태·생성 뷰 | 부분 저장·동시 writer·상태 불일치 0건 |
| 04 | `AGENT_STAGE_04_VERTICAL_SLICE_MIGRATION.md` | 첫 편집 수직 흐름 선별 이식 | 구/신 결과 비교 통과, 입력 변경 0건 |
| 05 | `AGENT_STAGE_05_SHADOW_PILOT.md` | 실제 영상 병행·리뷰 렌더·최소 복구 파일럿 | 골든 3회, 신규 실제 1회와 legacy 직접 1회 또는 승인된 동결 기준, A/V 승인·빈 경로 복구 통과 |
| 06 | `AGENT_STAGE_06_CUTOVER.md` | 전환 후보·가역적 임시 전환 | clean-clone·복구 훈련과 원격 CI 또는 명시적 로컬 전용 위험 승인 |
| 07 | `AGENT_STAGE_07_OPERATIONS_DOCUMENTATION.md` | 정기 백업·지표·MkDocs·안정화 | 실제 작업 10건과 30일, 복구·엄격 문서 빌드·자동 지표 통과 |
| 08 | `AGENT_STAGE_08_KNOWLEDGE_TOOLS_PILOT.md` | 선택적 Obsidian·Graphify | 측정 기준을 충족한 도구만 유지 |

단계는 건너뛰지 않는다. 한 단계가 실패하면 같은 단계에서 수정·재검증하거나 마지막
통과 단계로 롤백한다.

### 예상 작업량과 병렬 일정

- Stage 00~07 핵심 재구축: 약 15~22 인일
- 쓰기 범위가 겹치지 않는 최대 3개 역할을 병렬 배치할 때 예상 경과: 약 8~13 작업일
- Stage 07의 안정화 판정: 위 구현 기간과 별도로 실제 제작 10건 및 30일 관찰
- Stage 08 선택 도구 파일럿: 도구별 약 2~4일과 Obsidian 2주 사용 관찰

이는 계획값이며 Stage 00 기준선에서 실제 파일 수, 테스트 수, Premiere 수동 렌더 시간을
확인한 뒤 갱신한다. 인일 단축을 위해 승인·A/V 검토·복구 게이트를 생략하지 않는다.

## 7. 표준 작업 패킷

조정 에이전트는 하위 에이전트에게 다음 필드를 모두 전달한다.

```text
task_id:
stage:
role: implementer | verifier | red-team
objective:
preconditions:
read_scope:
write_scope:
forbidden_scope:
dependencies:
required_tests:
required_evidence:
stop_conditions:
rollback_point:
user_approval_id_or_message:
```

`write_scope`가 비어 있으면 읽기 전용 작업이다. 작업 지시서에 없는 변경이 필요해지면
범위를 임의 확대하지 않고 조정 에이전트에게 반환한다.

## 8. 단계 공통 수행 절차

1. 사전조건과 사용자 승인 확인
2. Git 상태, 현재 브랜치, HEAD, 예상하지 못한 변경 확인
3. 단계 입력 자료와 직전 단계 보고서 확인
4. 실패 재현 또는 특성 테스트 작성
5. 최소 범위 구현
6. 로컬 검증과 실패 경로 검증
7. 별도 검증 에이전트의 재실행
8. 변경 파일, 테스트 결과, 미해결 위험, 롤백 방법 기록
9. 사용자에게 결과 보고 후 다음 단계 승인 대기

## 9. 표준 증거 패키지

각 단계는 구현 시 다음 경로에 증거를 생성한다.

```text
reports/rebuild/stage-XX/
├─ STAGE_REPORT.md
├─ COMMAND_RESULTS.md
├─ FILE_CHANGES.md
├─ RISKS_AND_ROLLBACK.md
└─ evidence/
```

`STAGE_REPORT.md`에는 반드시 다음을 포함한다.

- 작업 당시 HEAD와 브랜치
- 사용자 승인 범위
- 담당 에이전트와 검증 에이전트
- 변경한 파일과 의도
- 실행한 검증과 실제 종료 코드
- 실패한 검증과 해결 여부
- `backup/` 및 원본 입력 무변경 증거
- 단계 지표의 기준값과 결과
- 다음 단계에서 금지할 행동
- 정확한 롤백 지점과 복구 절차
- 실패 증거 보존 위치와 해시
- 애플리케이션 버전, DB schema version, 마지막 migration의 호환 범위

로그가 길면 전체 로그를 그대로 문서에 붙이지 말고 핵심 결과와 원본 증거 경로를
기록한다.

## 10. 공통 중단 조건

다음 중 하나라도 발생하면 즉시 작업을 중단하고 사용자와 조정 에이전트에게 보고한다.

- `backup/` 또는 원본 입력에 예상하지 못한 변경 발생
- 실패한 하위 프로세스가 최종 성공으로 보고됨
- 현재 단계 밖의 공통 파일을 수정해야만 진행 가능
- 기존 사용자 변경과 충돌
- 상태 DB 마이그레이션 전에 안전한 복사본을 만들 수 없음
- 실제 미디어를 외부 서비스에 전송해야 함
- 유료 플랜 또는 보안 권한 변경이 필요함
- 골든 결과의 차이를 기술적으로 설명할 수 없음
- 복구 절차가 실제로 실행되지 않음

## 11. 성공 판정 지표

최초 10개 실제 제작 작업과 전환 후 30일 관찰 기간 동안 다음을 추적한다.

- 실패가 성공으로 보고된 횟수: 0
- 원본 입력 해시 변경: 0
- 승인 없는 단계 전이: 0
- 미등록 산출물: 0
- 상태 DB와 생성 뷰 불일치: 0
- clean-clone 설치·검사 성공: 5회 연속 100%
- 첫 수직 흐름 성공률: 초기 10건 90% 이상, 이후 95% 이상
- 동일 중대 회귀 재발: 0
- 핸드오프 후 재개 시간: 2분 이내
- 백업 복구 시간: 30분 이내
- 안정화 후 메타 작업 비율: 전체 작업 시간의 20% 이하

## 12. 계획 문서 사용 완료 기준

- 실행할 단계와 직전 승인 게이트가 명확히 식별됨
- 각 하위 에이전트에 표준 작업 패킷과 배타적 쓰기 범위가 배정됨
- 구현, 독립 QA, 레드팀 역할이 같은 사람 또는 에이전트의 자기 승인으로 합쳐지지 않음
- 각 단계 보고서에 실제 종료 코드, 보호 범위, 롤백 지점이 기록됨
- 다음 단계 착수 전 사용자 승인 필요 여부가 확인됨
- 단계 08까지 완료해도 `backup/`과 원본 입력 변경이 0건임

## 13. 현재 범위 밖의 항목

- `backup/` 삭제 또는 역사 정리
- 모든 구체계 도구의 일괄 복원
- Graphify·Obsidian의 즉시 상시 운영
- PostgreSQL, Temporal, Prefect 등 관리형 오케스트레이션 도입
- 대용량 영상 파일의 Git 또는 CI 아티팩트 업로드
- 사람 A/V 승인 자동화
