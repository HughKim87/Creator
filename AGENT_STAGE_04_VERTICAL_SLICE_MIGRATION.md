# 단계 04 작업 지시서: 첫 수직 기능 이식

## 1. 단계 목적

구체계 파일 전체가 아니라 실제 영상 제작에 필요한 한 줄의 흐름을 신규 구조에 끝까지
이식한다. 첫 범위는 다음으로 고정한다.

```text
소스 등록
→ source_id와 원본 지문 검증
→ 동기화 검사
→ Premiere XML 생성
→ 산출물 등록과 해시 검증
```

## 2. 시작 게이트

- 단계 03까지의 로컬 전체 검증이 통과했다.
- 단일 상태 DB와 생성 뷰가 복구 시험을 통과했다.
- 단계 00 골든 비교 대상과 이식 목록이 있다.
- 실제 미디어 생성·사용이 필요한 경우 사용자 승인을 받았다.
- 구체계 보존 태그 또는 정확한 SHA가 있다.

## 3. 역할과 파일 소유권

### 분석 에이전트

- 읽기 전용으로 이식 대상 코드·테스트·입출력 계약을 조사한다.
- 구체계 파일별 책임, 부작용, 오류 계약을 작성한다.

### identity 구현 에이전트

- `domain/identity.py`
- `services/source_registration.py`
- 관련 테스트

`source_registration`은 총괄 에이전트가 먼저 동결한 `MediaProbe` 포트만 사용한다.
`media_probe.py` 구현이 완료되기 전에는 임시 구현을 각자 만들지 않는다.

### sync 어댑터 에이전트

- `adapters/media_probe.py`
- `adapters/sync_check.py`
- 관련 테스트

### Premiere 어댑터 에이전트

- `adapters/premiere_xml.py`
- 관련 테스트

### artifact integration 에이전트

- `services/artifact_service.py`
- storage artifact repository adapter
- `artifact reconcile`과 관련 CLI 연결
- 승격·복구·실패 주입 테스트

### QA 에이전트

- 골든·회귀·수직 스모크·실패 주입 테스트
- 프로덕션 코드 수정 금지

공개 도메인·저장소 인터페이스와 `MediaProbe` 포트를 먼저 동결한다. 동기화와 XML
어댑터는 파일 범위가 분리되면 병렬 가능하다. 공용 CLI와 DB 인터페이스의 최종 통합은
조정 에이전트만 수행한다.

## 4. 조사 대상

최소한 다음 구체계 영역을 확인한다.

- 소스 프레임 생성·등록 도구
- `synccheck` 관련 도구와 테스트
- Premiere XML 생성 스크립트와 테스트
- 편집 품질 감사
- workflow gate와 산출물 registry
- source identity, generation lock, 승인 계약

각 기능에 대해 입력, 출력, 불변조건, 오류 조건, 외부 의존성, 부작용, 현재 테스트,
이식 결정과 근거를 표로 남긴다.

## 5. 금지 사항

- `backup/` 코드 수정
- 신규 runtime이 `backup/` Python을 import하거나 실행 경로로 사용
- 구체계 전체 디렉터리 복사
- 테스트 없이 알고리즘 재작성
- 원본 입력 수정·이동·재인코딩
- MP4 생성에 대한 사용자 승인 우회
- 구체계 출력과 신규 출력을 같은 디렉터리에 생성
- 실패 산출물을 현재 산출물로 승격

## 6. 보존할 동작 계약

### 소스 식별

- 한 지문은 하나의 `source_id`에만 대응한다.
- 같은 `source_id`에 다른 지문을 재사용하지 않는다.
- 등록 시 SHA-256, 크기, 미디어 메타데이터를 저장한다.
- 등록 후 입력 해시가 달라지면 진행을 차단한다.
- 임시·백업·출력 경로를 입력 원본으로 등록하지 않는다.

### 프레임·시간 좌표

- 프레임 자산은 원본 소스 시간과 프레임 번호로 추적한다.
- 타임라인 위치를 원본 시간으로 잘못 저장하지 않는다.
- 동일 조건의 안전한 고해상도 프레임만 재사용한다.

### 동기화 검사

- 오디오 신호 검사와 연속 A/V 검토를 별도 결과로 기록한다.
- 한 검사의 성공이 다른 검사의 실패를 덮어쓰지 않는다.
- 자동 수치와 증거 경로를 저장하고 사람 승인을 분리한다.
- 실패 결과도 역사 증거로 보존한다.

### Premiere XML

- 소스·타임라인 프레임 길이와 FPS가 명확하다.
- 시퀀스는 0에서 시작하고 순서가 결정적이다.
- 중복 ID, 0프레임 구간, 허용되지 않은 미디어 혼합을 거부한다.
- 컷어웨이가 원본 오디오를 의도치 않게 끊지 않는다.
- 같은 입력에 의미상 같은 XML을 생성한다.

### 산출물

모든 산출물은 프로젝트, 세대, 소스, 역할, 경로, 해시, 크기, 도구 버전, 부모 계보와
검증·lifecycle 상태를 가진다. 승인 정본은 `approvals`이며 산출물의 승인 여부는
승인 레코드에서 파생한다.

## 7. 세부 작업

### 작업 04-1: 구체계 특성 테스트 동결

구체계 코드를 고치기 전에 대표 입력으로 다음 결과를 수집한다.

- 소스 등록 결과
- 프레임 이름과 매니페스트
- 동기화 검사 결과
- Premiere XML
- 오류 메시지와 종료 코드

절대 경로, 생성 시각, 임의 UUID처럼 실행마다 다른 값은 정규화 규칙을 먼저 문서화한다.
기대 결과를 신규 구현에 맞춰 임의로 바꾸지 않는다.

### 작업 04-2: source identity 이식

- 동일성·해시 규칙은 domain에 둔다.
- 파일 읽기와 FFprobe는 adapter에 둔다.
- 서비스는 파일 지문 검증과 DB 등록을 하나의 논리 작업으로 조정한다.
- 등록 도중 파일이 바뀌면 실패하고 부분 레코드를 남기지 않는다.

부정 테스트: 존재하지 않는 파일, 0바이트, 금지 경로, ID·지문 충돌, 등록 중 변경,
한글·공백 경로.

### 작업 04-3: 동기화 검사 이식

구체계 코드를 순수 계산, 외부 실행, 경로 결합, 프로젝트 상수, 임시 실험으로 분류한다.
순수 계산만 먼저 이식하고 외부 실행은 어댑터로 감싼다.

각 검사 결과는 scope, status, metrics, evidence, failure code를 구조화해서 반환한다.
예외를 문자열 성공으로 변환하지 않는다.

### 작업 04-4: Premiere XML 이식

CLI·파일 읽기와 XML 생성 로직을 분리한다. XML 텍스트뿐 아니라 의미 구조를 비교한다.
공백·속성 순서 차이는 정규화할 수 있지만 프레임, 경로, 트랙 관계 차이는 실패다.

필수 범위: 여러 소스, 프레임 경계, 컷어웨이, A/V 범위 차이, 잘못된 FPS, 중복 ID,
빈 컷, 상대·절대·한글 경로.

### 작업 04-5: 산출물 장애 일관 승격

SQLite 커밋과 파일 시스템 이동은 하나의 원자적 트랜잭션이 아님을 전제로 한다.
`원자적 승격`이라고 보고하지 않고 다음 복구 가능한 프로토콜을 구현한다.

1. DB 트랜잭션으로 고유 artifact ID와 `staging` 레코드를 예약한다.
2. 최종 경로와 같은 볼륨의 세대별 임시 디렉터리에 파일을 생성한다.
3. 파일을 완전히 flush한 뒤 존재·크기·구문·SHA-256을 검증한다.
4. 동일 볼륨에서 `os.replace` 또는 동등한 원자적 파일 교체로 최종 경로에 놓는다.
5. 별도 DB 트랜잭션에서 실제 경로·해시를 재검증하고 artifact를 `ready`로 바꾸며
   상태 이벤트를 기록한다.
6. 어느 경계에서든 실패하면 성공으로 보고하지 않고 `failed` 또는 `quarantined`로
   남겨 reconciliation 대상이 되게 한다.

`workflow artifact reconcile`은 자동 삭제 없이 다음을 탐지하고 구조화된 해결 후보를
제시한다.

- DB `staging` 레코드는 있으나 임시·최종 파일이 없음
- 최종 파일은 있으나 DB가 `staging`에 머묾
- 파일은 있으나 DB 레코드가 없음
- DB와 파일 해시 불일치
- 중단된 실행의 오래된 `staging` artifact

자동 복구는 해시와 계보가 확정된 `staging → ready`에만 제한한다. 그 밖의 파일은
격리하며 삭제·덮어쓰기 전에 사용자 승인을 받는다. XML 생성은 성공했지만 등록·검증이
실패하면 명령 전체를 실패로 판정한다.

## 8. 테스트 계층

- 단위: 식별자, 시간 좌표, 순수 동기화 계산
- 계약: CLI 입력·출력·종료 코드
- 어댑터: FFmpeg/FFprobe, 파일 경로, XML
- 회귀: 구체계에서 발견한 결함
- 골든: XML과 매니페스트의 의미 비교
- 수직 스모크: 등록부터 산출물 승격까지
- 실패 주입: staging 예약 전·후, 파일 생성·flush·replace 전·후, ready 커밋 전·후,
  파일 결손, 외부 명령 실패, DB 오류
- reconciliation: DB만 있음, 파일만 있음, 해시 불일치, 오래된 staging을 각각 탐지

## 9. 완료 기준

- 신규 코드의 `backup/` import·runtime 참조 0건
- `backup/`과 원본 입력 변경 0건
- 구체계 골든 결과와 설명되지 않은 의미 차이 0건
- 등록되지 않은 산출물 0건
- `ready`인데 파일이 없거나 해시가 다른 산출물 0건
- 해결되지 않은 `staging`과 미격리 orphan 파일 0건
- 실패가 성공으로 보고된 사례 0건
- 수직 스모크 3회 연속 통과
- Windows 한글·공백 경로 통과
- 모든 산출물 계보 조회 가능
- 독립 QA와 레드팀 승인

## 10. 중단 조건

- 골든 차이를 기술적으로 설명할 수 없음
- 테스트를 이유 없이 신규 결과에 맞춰 변경함
- 원본 또는 구체계 파일 변경 발생
- 신규 도메인 인터페이스를 우회해야만 이식 가능
- 동일 실패를 변경 없이 세 번 반복함

## 11. 롤백

- 실패 세대는 `aborted`로 기록하고 현재 세대로 승격하지 않는다.
- 산출물은 매니페스트 기준으로만 격리하며 계산 경로 확인 없는 재귀 삭제를 금지한다.
- 신규 기능을 단계 03 마지막 통과 지점으로 되돌린다.
- 구체계는 보존 SHA의 별도 worktree에서 비교용으로만 실행한다.
- 신규 DB를 구체계 형식으로 덮어쓰지 않는다.
