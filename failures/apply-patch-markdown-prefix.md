# `apply_patch`와 Markdown 목록 기호 충돌

- 상태: 해결·회귀 검증 완료
- 최초 확인: 2026-07-23 다단계 문서 일괄 보정
- 마지막 검증: 2026-07-23 문서 기반 데이터 규칙 보정
- 적용 범위: 패치 형식으로 Markdown 목록을 추가하는 문서 작업

## 증상

여러 단계 문서에 목록 문장을 일괄 추가하는 패치가 형식 오류로 두 번 거부됐다. 두 시도 모두 파일에는 적용되지 않았다.

## 확인된 원인

패치의 추가 행 표시 `+`와 Markdown 목록의 `-`를 분리해 작성하지 않아, 추가하려는 목록 행이 유효한 패치 행으로 해석되지 않았다.

## 실패·해결 이력

| 시점·문맥 | 시도·결과 | 최고 연속 횟수 | 해결·검증 |
|---|---|---:|---|
| 다단계 문서 일괄 보정 | 형식 오류 2회, 모두 무적용 | 2 | 추가 행을 `+- 목록 내용` 형태로 교정해 적용 성공, 대상 문서 재검사 후 활성 횟수 0 |
| Stage 04 시각 검증 보정 | 두 `Update File` 구간 사이의 불필요한 hunk 표식 때문에 다음 파일 표식이 본문으로 해석돼 무적용 | 1 | 파일별 update 구간을 분리하고 patch 접두사와 기존 문맥을 다시 확인 |
| Stage 04 계약 일괄 추가 | 기존 실패 표의 문구를 다르게 가정해 전체 패치 문맥 검증 실패·무적용 | 1 | 실제 파일을 다시 읽고 정확한 기존 행을 기준으로 패치를 분리해 적용 |
| Stage 04 테스트·실패 문서 동시 보정 | 실패 문서의 실제 절 제목과 다른 문맥을 사용해 전체 패치 검증 실패·무적용 | 1 | 대상 문서를 다시 읽고 실제 표와 목록 문맥을 기준으로 분리 적용 |
| Stage 05 결정 유형 export 추가 | 닫는 `__all__` 목록과 일치한 모호한 문맥 뒤에 export 문자열 3개가 삽입돼 전체 테스트 모듈 import 실패 | 1 | 파일 전체를 다시 읽고 문자열을 실제 `__all__` 내부로 이동, AST와 전체 테스트 재검증 |
| Stage 05 인코딩 실패 이력 추가 | Stage 03 이력 행의 실제 문구를 다르게 가정해 2개 파일 묶음 패치가 무적용 | 1 | 실제 실패 문서를 다시 읽고 기존 행을 기준으로 적용, 대상 두 파일 재검사 |
| 문서 기반 데이터 규칙 실패 이력 보정 | 실패 문서의 마지막 목록 문맥을 실제 내용과 다르게 지정해 전체 패치가 무적용 | 1 | 무적용을 확인하고 이미 읽은 정확한 표·목록 문맥으로 작은 hunk를 재적용 |
| Stage 10 10R-B 여러 파일 동시 보정 | 첫 파일 hunk 뒤 불필요한 빈 `@@`가 남아 다음 `Update File` 표식이 hunk 본문으로 해석돼 전체 패치 무적용 | 1 | 빈 hunk를 제거하고 각 파일 update 구간을 연속 배치해 재적용, 세 대상 파일 변경과 maintenance·diff 검증 성공 |

## 해결과 검증

- 패치 제어 기호와 문서 본문의 목록 기호를 별도 문자로 검토했다.
- 실패 시도가 무적용임을 확인한 뒤 수정 패치를 적용했다.
- 모든 단계 문서에 목표 문장이 존재하는지 다시 검색하고 diff를 검사했다.

## 재사용 규칙

- Markdown 목록 추가 행은 패치의 `+` 다음에 본문의 `-`가 오는지 확인한다.
- 일괄 패치 전에 한 문서의 작은 패치로 형식과 문맥을 확인한다.
- 패치 실패 뒤 재시도하기 전에 부분 적용 여부를 먼저 확인한다.
- 여러 파일을 한 패치에 넣을 때 각 `Update File` 구간 사이에 불필요한 `@@`를 두지 않고, 기존 문맥은 먼저 실제 파일에서 확인한다.
- 같은 닫는 구문이 여러 번 나오는 export·등록 목록은 삽입 후 즉시 파일 전체 또는 AST를 확인한다.

## 근거

- [마스터 구축 계획](../docs/build/MASTER_BUILD_PLAN.md)
- [Stage 00 자체 검토](../docs/build/stage-00-project-kernel.md#111-검토에서-발견수정한-결함)
