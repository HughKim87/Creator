# 프로젝트 방향

- 문서 분류: `reference-evidence`
- 역할: 사용자가 여러 Codex 작업에서 직접 설명한 장기 결과와 설계 판단 기준의 단일 방향 owner.
- 상태: `active`; 2026-07-30에 관련 Codex 작업의 직접 사용자 발언을 다시 대조해 교정했다.
- 독자: 프로젝트 방향을 설계·검토하는 사용자와 AGENT.
- 권위: 최신 사용자 지시와 `PROJECT_RULES.md`가 우선한다. 이 문서는 정책·작업 승인·현재 상태·실행 계획이 아니다.
- 유지 방법: 사용자 직접 발언으로 확인된 의미만 보존한다. 에이전트 요약·보고서·기존 설계만으로 새 방향을 추가하지 않는다.

## 근거 범위

현재 방향은 다음 Codex 작업의 직접 사용자 발언을 종합한 것이다.

- `019fa4d3-728d-7c03-91ef-67cb0628148e` — 불필요한 프로젝트 파일 찾기
  - 특정 영상 하나가 아니라 반복되는 영상 작업을 위한 프로젝트
  - 과거 자료에서 재사용 가능한 규칙·실패 지식을 추출한 뒤 불필요한 문서를 정리
  - 새 컴퓨터 재현성, 규칙 기반 품질, 자동화, 설계→실행, 경험 환류, 복잡성 자체검증, Core 이식
  - 짧고 단계적인 설계, 단계별 성공 gate, 다음 AGENT가 읽을 수 있는 문서 구조
- `019fa9f6-3956-7ac3-8a87-a9461be04da3` — 프로젝트 개선작업 M0~M6 실행
  - 단계 gate 뒤 커밋, 권장안 자동 선택, 불필요한 규칙·문서 증가 자체검증
  - 다른 AGENT에게 위임해도 목표와 의도를 잃지 않는 설계
- `019faf75-037f-7e92-ac69-420f40b82324` — 중단된 개선작업 재개
  - 데이터 기반 판단은 직접 근거와 확실한 출처 구분이 선행
  - 현재 문서 정리 이니셔티브에서는 문서 제거가 1차 목표
  - 실제 제거는 다른 개선 작업이 모두 끝난 뒤 exact 목록 승인 후 수행
- 2026-07-31 현재 Codex 작업 — 프로젝트 전역 정리 범위 교정
  - 정리 범위는 ignored 파일만이 아니라 tracked·untracked·ignored, 백룸 작업과 개선 과정에서 생성된 파일, `outputs/`·`extension/data/`를 포함한 작업트리 전체
  - 모든 내용을 무조건 흡수하지 않고 이후 workflow에 재사용할 수 있는 지식·계약·검증·실패 경험만 선별
  - 재사용 항목은 기존 canonical owner에 우선 흡수·병합하고, 기존 owner로 책임을 표현할 수 없을 때만 최소 owner 신설
  - 최종 결과는 Git 상태뿐 아니라 남은 로컬·ignored 산출물까지 용도와 owner가 설명되는 clean workspace

위 범위에서 확인되지 않은 우선순위나 목표는 임의로 채우지 않는다.

## 한 문장 방향

이 프로젝트는 **새 컴퓨터에서도 규칙에 따라 일정한 품질의 영상 작업을 재현하고, 설계·실행·검증·경험 환류·복잡성 감사를 연결해 작업을 점점 빠르고 간결하게 자동화하며, 도메인 중립 Core를 다른 종류의 프로젝트에도 이식할 수 있는 운영 기반**을 구축한다.

## 사용자가 요구한 통합 결과

- 특정 영상 하나가 아니라 여러 YouTube·영상 작업에 반복 적용되는 workflow와 규칙을 만든다.
- 새 컴퓨터의 clean clone에서도 필요한 환경과 검증을 재현하고 일정 수준의 결과 품질을 보장한다.
- 조사·기획·제작·검증·업로드 준비가 자동 workflow로 이어지고, 승인된 범위에서는 AGENT가 안전한 기본값과 실패 복구를 스스로 수행한다.
- 모든 material 작업을 `설계·기획 → 실행`로 나누고 단계마다 목표·범위·성공 gate·중단 조건을 명확히 한다.
- 실행 결과와 과거 성공·실패를 분석해 재사용 가능한 규칙으로 환류하고, 반복할수록 행동과 문맥을 간결하게 만든다.
- 규칙·문서·자동화가 늘어날 때는 과도한 시스템과 유지 비용을 스스로 검증하고, 효과가 없는 복잡성을 줄인다.
- `core/`에는 도메인 중립 기반을, `extension/`에는 YouTube·영상 작업을 두고, Core를 게임 개발 같은 다른 프로젝트에 이식해 재사용성을 검증한다.
- 과거 문서의 재사용 가능한 지식은 canonical owner로 옮기고, 역할이 끝난 원본 문서는 의존성 해제와 exact 사용자 승인 뒤 활성 트리에서 제거한다.
- 백룸·개선 작업의 중간물과 Git 상태에 관계없이 작업트리 전체를 같은 선별 기준으로 감사하며, 재사용 가치가 없는 항목은 owner·참조·복구·승인 확인 뒤 정리한다.
- 최종 workspace에는 재현·운영·사용자 산출물에 필요한 파일만 남고, tracked 변경과 설명되지 않은 untracked·ignored·임시 산출물이 없어야 한다.

이 결과들은 서로 대체 관계가 아니다. 실제 영상 산출물만 만들고 재현 가능한 기반을 포기하거나, 기반만 정교하게 만들고 실제 workflow를 완성하지 않는 상태 모두 목표 미달이다.

## 설계와 실행의 판단 기준

- 보안·보호 데이터·권한 경계는 항상 먼저 지킨다.
- 사용자 직접 발언, 1차 파일·테스트·실측, 에이전트 해석을 서로 다른 evidence로 표시한다.
- 검증되지 않은 전제를 목표나 승인으로 승격하지 않고 `unresolved`로 남긴다.
- 계획 준비가 실행보다 길어지지 않도록 전체 설계는 단계 지도만, 단계 설계는 현재 실행에 필요한 계약만 소유한다.
- 다음 AGENT는 대화 원문 없이도 목표·현재 단계·금지사항·성공 gate·첫 행동을 복원할 수 있어야 한다.
- 숫자·문서 수·테스트 수 자체가 아니라 재현성, 품질, 자동화, 작업 시간, 재작업, 이식성, 복잡성 감소로 성공을 판정한다.
- 문서 정리 작업에서는 지식 추출과 검증을 먼저 끝내고, 실제 제거는 다른 개선 작업 종료 뒤 별도 승인 단계에서 수행한다.
- “재사용 가능”은 반복 workflow의 조건·행동·예외·검증을 강화하는 경우이며, 단일 작업 사실·중복 설명·재생성 가능 파생물은 무조건 규칙이나 문서로 승격하지 않는다.

## 성공을 판단하는 증거

- ASCII·한글·공백 경로의 clean clone에서 환경 확인과 Core·Extension 검증이 재현된다.
- 같은 유형의 영상 작업이 규칙과 workflow를 통해 반복 가능한 품질의 사용자용 산출물로 이어진다.
- 설계와 실행이 단계 gate로 연결되고, 다음 AGENT가 동일한 목표와 범위로 재개한다.
- 성공·실패 경험이 검증된 규칙으로 환류해 반복 작업의 수동 입력·시간·재작업을 줄인다.
- 규칙·필수 문서·도구의 증가가 실제 효과와 함께 측정되고, 불필요한 복잡성과 완료 문서가 정리된다.
- Core가 YouTube Extension에 역의존하지 않고 다른 도메인 Extension에서도 동일한 계약과 검증을 통과한다.
- 제거된 문서는 재사용 지식·승인·계보의 유일한 owner가 아니며 Git 또는 승인된 복구 경계로 회복 가능하다.
- 프로젝트 전역 inventory에서 모든 파일이 유지·흡수 후보·보호 보존·재생성 가능·처분 후보·미해결 중 하나로 설명되고, 최종 allowlist 밖의 ignored·untracked 항목이 0이다.

## 다른 문서와의 경계

| owner | 소유하는 것 |
|---|---|
| `PROJECT_RULES.md` | 항상 적용되는 정책·권한·보호 경계와 조건부 읽기 route |
| 이 문서 | 직접 사용자 발언으로 확인된 장기 결과와 판단 기준 |
| active overall/phase design | 현재 initiative의 exact 목표·범위·단계·gate |
| `SESSION_HANDOFF.md` | 검증된 현재 상태·blocker·첫 다음 행동 |
| `README.md` | 사용자와 새 참여자를 위한 시작 경로·구조 개요 |
| Git | 완료 과정·과거 원문·복구 가능한 diff |

이 문서는 Core 변경, 보호 데이터 접근, commit·push·게시·설치·비용 지출을 승인하지 않는다.
