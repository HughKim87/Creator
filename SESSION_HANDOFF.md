# 세션 핸드오프

- 갱신일: 2026-07-22
- 역할: 채팅 기억 없이 다음 세션이 현재 검증 상태에서 재개하기 위한 단일 활성 상태 정본
- 현재 단계: 00~09단계와 Stage 01.5 설계·계획 완료, 구현 미착수
- 다음 구현 후보: Stage 00 — 최소 프로젝트 기반
- 승인 상태: 단계 구성, 계획 문서 작성, Stage 01.5 Obsidian 설계 반영은 승인됨. Stage 00 구현 착수와 단계별 미확정 정책은 아직 승인되지 않음
- 백업: 별도 복제 없음. 역사 프로젝트 규칙의 중복 백업 금지를 따르며 분석 근거는 기존 보고서에 보존됨

## 1. 시작 목적과 읽기 순서

다음 세션은 아래 순서로 시작한다.

1. 대화 시작 지침에 따라 각 역사 프로젝트의 `PROJECT_RULES.md`를 끝까지 읽는다.
   - `backup/origin/PROJECT_RULES.md`
   - `backup/refactorying_snapshot_1/PROJECT_RULES.md`
   - `backup/refactorying_snapshot_2/PROJECT_RULES.md`
   - `backup/refactorying_snapshot_3/PROJECT_RULES.md`
2. 이 `SESSION_HANDOFF.md`를 읽는다.
3. `reports/2026-07-22_프로젝트_의도_통합_요구사항_기준서.md`를 현재 요구사항 기준으로 읽는다.
4. `docs/build/MASTER_BUILD_PLAN.md`에서 단계 순서와 현재 단계를 확인한다.
5. 구현을 요청받은 경우에만 현재 단계 문서 `docs/build/stage-00-project-kernel.md`를 읽는다.

Codex·Claude 역추론 보고서와 `backup/`은 근거를 다시 확인할 때만 좁게 읽는다. 역사 프로젝트의 실행 경로를 활성 루트의 실행 지시로 사용하지 않는다. `inputs/`, `outputs/`, 비밀정보, 인증정보는 사용자가 정확한 대상과 목적을 지정하지 않는 한 열거·열람·요약하지 않는다.

## 2. 현재 목표

현재 최상위 목표는 영상 제작 워크플로 구현이 아니다.

**작업 결과·실패·판단·경험을 출처와 함께 선별 축적하고, 다음 작업에 필요한 지식만 효율적으로 읽고 다시 결과에 반영하는 데이터 기반 지식 운영 기반을 계층적으로 구축하는 것**이 현재 목표다.

구축 순서는 다음과 같다.

> 공통 규칙 → 정보·문서 책임 → Obsidian 문서 가시화·검토 → 파일 기반 데이터 → 읽기·쓰기 → 작업 상태·파일 관리 → 지식 유형과 수명주기 → 선택적 컨텍스트 → 유지보수 → 도메인 워크플로

최종 형태를 처음부터 축소 구현하지 않는다. 각 단계는 이전 단계 결과만 사용하고, 관련 단계의 불명확한 정책·기술·수치는 사용자에게 질문한 뒤 구현한다.

## 3. 현재 권위 구조

| 우선순위 | 문서·근거 | 소유 범위 |
|---:|---|---|
| 1 | 사용자의 최신 명시적 원문과 확인 | 목적, 범위, 승인, 금지, 예외 |
| 2 | `reports/2026-07-22_프로젝트_의도_통합_요구사항_기준서.md` | 현재 요구사항과 수용 기준 |
| 3 | `docs/build/MASTER_BUILD_PLAN.md` | 구축 순서, 단계 경계, 공통 게이트 |
| 4 | `docs/build/stage-00-*.md` ~ `stage-09-*.md` | 각 단계의 설계·구현 계획 |
| 5 | Codex·Claude 역추론 보고서 | 분석 근거와 역사 해석 |
| 6 | `backup/` | 읽기 전용 역사 증거 |

같은 내용을 여러 문서에 다시 쓰지 않는다. 요구사항은 통합 기준서, 구현 순서는 마스터 계획, 현재 상태는 이 핸드오프가 각각 소유한다.

## 4. 사용자 확인으로 확정된 결정

1. 현재 요구사항의 기준은 Codex 역추론 보고서다.
2. Claude 보고서에서는 인지 복잡성, 승인 실패, 사용자 원문 보존 분석을 병합한다.
3. 현재 최상위 목적은 데이터 기반 지식 운영체계의 기반이다.
4. 영상 제작은 기반 완료 후 별도 승인으로 결합할 후속 도메인이다.
5. 과거 지식 전체를 이관하지 않고 현재 목적에 필요한 지식만 선택적으로 승계한다.
6. 구현은 00~09단계와 Stage 01.5로 나눠 순차적으로 확장한다.
7. 현재 단계에 필요하지 않은 미래 기능을 미리 구현하지 않는다.
8. 단계별 불명확한 내용은 에이전트가 임의로 채우지 않고 사용자에게 질문한다.
9. 상세 문서는 유지하되 항상 읽는 문서와 조건부로 읽는 문서를 분리한다.
10. 사용자의 인지 복잡성을 토큰 비용보다 먼저 관리한다.
11. 승인은 상태값이 아니라 사용자가 변화와 영향을 이해하는 의미 있는 단계 결정이어야 한다.
12. 목적·범위·금지·승인에 영향을 주는 사용자 원문은 에이전트 요약보다 높은 권위를 가진다.
13. 모든 단계는 [마스터 계획 §6.3](docs/build/MASTER_BUILD_PLAN.md#63-모든-단계의-네-가지-확인-규칙)의 네 가지 확인을 근거와 함께 모두 통과해야 완료할 수 있다.
14. Stage 01에서 문서 책임과 권위를 확정한 뒤 Stage 01.5에서 Obsidian 전체 문서 지도와 단계별 검토 환경을 구축하고, Stage 02부터 사용자가 이를 보며 각 단계를 진행한다.

## 5. 완료된 작업

### 5.1 분석과 요구사항

- Codex와 Claude의 프로젝트 의도 역추론 보고서 2건을 비교했다.
- 현재 요구사항 기준은 Codex 보고서로 선택됐다.
- Claude 보고서의 세 분석을 병합한 통합 기준서를 작성했다.
- Codex 필수 요구사항 M-01~M-20이 통합 기준서 UR-01~UR-15에 모두 대응되고, 사용자가 추가 확정한 Obsidian 문서 가시성 요구가 UR-16으로 분리된 것을 확인했다.
- 통합 기준서는 요구사항을 즉시 적용 제약, 단계별 기능, 조건부 확장, 사용자 결정 필요로 분리한다.

### 5.2 단계별 구축 계획

- `docs/build/MASTER_BUILD_PLAN.md`를 작성했다.
- Stage 00~09와 Stage 01.5의 단계별 설계·구현 계획 문서 11개를 작성했다.
- 각 단계 문서에 목표, 선행조건, 기능, 산출물, 순서, 제외 범위, 검증, 완료 조건, 사용자 결정, 다음 단계 인계를 정의했다.
- 구현 순서의 정본은 역추론 보고서가 아니라 마스터 계획으로 확정했다.
- 사용자의 2026-07-22 지시에 따라 모든 단계가 ① 사용자 목적 정합성, ② 실제 기능 작동, ③ 미래 단계 선행 유입 방지, ④ 과거 실패 패턴 재발 방지를 근거와 함께 확인하도록 공통 완료 규칙을 마스터 계획에 추가했다.
- 사용자가 모든 활성 문서·규칙과 연결 관계를 직접 보며 각 단계를 진행하도록 Stage 01과 Stage 02 사이에 `docs/build/stage-01-5-obsidian-document-visibility.md`를 추가하고 인접 단계와 요구사항을 연결했다.
- Stage 01.5 설계와 관련 문서 7개 변경은 Git 커밋 `783a1ff` (`docs: Obsidian 문서 검토 단계 추가`)에 보존됐다.

### 5.3 구현 상태

- 코드, 스키마, 데이터 저장 구조, CLI, 검색, 지식 코퍼스, 자동화는 구현하지 않았다.
- Stage 00도 아직 구현하지 않았다.
- Obsidian 설치·볼트 구성·플러그인 도입은 수행하지 않았다.
- 파일 삭제·이동, Git push, 외부 변경은 수행하지 않았다. 설계 문서 변경은 사용자의 요청에 따라 Git으로 기록한다.

## 6. 검증 상태

| 대상 | 검증 수준 | 확인 결과 |
|---|---|---|
| `reports/2026-07-22_프로젝트_의도_통합_요구사항_기준서.md` | 구조 검증 | UTF-8 유효, NUL 0, 내부 링크 오류 0, M-01~M-20 대응 확인 |
| `docs/build/MASTER_BUILD_PLAN.md` | 구조 검증 | 단계 00~09와 Stage 01.5 및 단계 문서 경로 존재, 통합 기준서 참조 반영, 네 가지 확인 규칙과 완료 차단 조건 존재 |
| `docs/build/stage-00-*.md` ~ `stage-09-*.md`, `docs/build/stage-01-5-obsidian-document-visibility.md` | 구조 검증 | 필수 계획 절과 파일 간 링크 확인. 구현 결과나 Obsidian 앱 검증은 아님 |
| Git 커밋 `783a1ff` | 버전 관리 검증 | Stage 01.5 신규 문서와 요구사항·마스터·인접 단계·핸드오프 변경 7개 파일 포함. 원격 push는 수행하지 않음 |
| `SESSION_HANDOFF.md` | 구조 검증 | UTF-8 유효, NUL 0, 필수 절 존재, 참조 경로 오류 0, 마스터·통합 기준서와 구현 상태 일치 |

문서 검증은 내용에 대한 사용자 승인이나 구현 완료를 의미하지 않는다. 실제 기능 검증은 아직 존재하지 않는다.

## 7. 실패 원장

연속 실패 횟수는 현재 사용자 실행 요청마다 다시 계산한다. 아래 수치는 역사 증거이며 현재 요청의 활성 실패 횟수는 모두 0이다.

| 목표 | 시도·버전 | 결과·확인 원인 | 역사 횟수 | 다음 조건 |
|---|---|---|---:|---|
| 현재 최상위 의도 복원 | 초기 Codex 보고서 해석 | 영상 제작을 현재 목표로 과대 해석했으나 사용자 확인으로 지식 기반 우선으로 정정 | 1 | 최신 사용자 원문과 통합 기준서를 최상위 근거로 사용 |
| 과거 지식 범위 판정 | 이전 지식 시스템 감사 | “모든 경험”을 과거 전체 이관으로 확대 해석 | 1 | 과거 자료는 목적에 맞는 후보로만 취급하고 선택 이유를 기록 |
| 구현 시작 방식 | 최초 문서·데이터 체계 제안 | 완성된 최종 구조를 시작 단계부터 요구해 사용자가 중단 | 1 | 마스터 계획의 계층 순서와 단계별 활성화 기준 준수 |
| 승인 유효성 | 역사 변경 제안과 다단계 진행 | 승인 절차는 있었지만 사용자가 승인 대상을 기억하기 어려워 실제 결정으로 작동하지 않음 | 1 | 승인 요청에 변화·영향·제외 범위·다음 정지 지점을 명시 |
| 핸드오프 최신성 | 2026-07-22 17시 핸드오프 | 이후 생성된 통합 기준서와 구축 계획을 반영하지 않아 오래된 다음 행동과 존재하지 않는 보고서 경로가 남음 | 1 | 현재 갱신 후 링크·정본 검증, 이후 중요 상태 변경 시 동시 갱신 |
| 설계 변경 Git 커밋 | Stage 01.5 설계 커밋 최초 시도 | 기본 샌드박스에서 `.git/index.lock` 쓰기 권한이 거부됐으나 승인된 Git 쓰기 경계로 재실행해 `783a1ff` 커밋 성공 | 1, 성공으로 해소 | 사용자가 커밋을 승인한 경우 확인된 Git 쓰기 경계를 사용하고, 성공 후 커밋 내용을 재검증 |

같은 목표의 현재 요청 내 3회 연속 실패는 없다.

## 8. 활성 차단 요소와 위험

### 8.1 Stage 00 구현 전 사용자 결정

Stage 00 구현을 시작하려면 다음을 사용자에게 질문해야 한다.

1. 매 대화 시작 시 반드시 읽을 활성 문서의 최대 범위
2. 상시 규칙에 반드시 포함할 사용자 지정 규칙
3. `README.md`의 주 독자를 사용자로 한정할지 신규 에이전트까지 포함할지
4. 현재 시스템의 전체 그림을 어떤 형식으로 보여주고 인지 복잡성을 어떻게 점검할지
5. 승인 기록의 소유 문서와 과거 승인을 다시 확인할 조건

기존 핸드오프의 긴 역사 내용을 축약할 수 있는지는 이번 “핸드오프 작성” 요청으로 해소됐다. 필요한 역사 근거는 보고서 링크로 남기고 현재 상태에는 복제하지 않는다.

### 8.2 이후 단계까지 보류할 결정

- 사용자 원문으로 보존할 발언의 범위, 저장 위치, 보존 기간: Stage 01
- Obsidian 볼트 범위, 편집 권한, 설정 추적, 설치·실행 방식, 사용자 검증 과제: Stage 01.5
- 대표 지식 작업과 승격·보존·폐기 기준: Stage 05~06
- 컨텍스트 토큰 기준선·상한·절감 목표: Stage 07
- 세 대표 실사용 시나리오의 최종 합격 수치: Stage 07~08

### 8.3 현재 위험

- 최종 요구사항을 모두 현재 단계의 구현 지시로 오독할 위험
- 문서 수나 테스트 수를 사용자 가치의 완료 증거로 사용할 위험
- 보고서·마스터·단계 문서·핸드오프가 같은 상태를 중복 소유할 위험
- 인지 복잡성 관리를 이유로 또 다른 대형 관리 시스템을 만들 위험
- 사용자 원문 보존을 모든 대화와 모든 과거 자료의 전량 보존으로 확대할 위험
- 네 가지 확인을 실제 근거 없이 형식적인 체크리스트로만 통과시킬 위험
- Obsidian 지도나 대시보드가 기존 정본을 복제해 또 다른 상태 소유자가 될 위험
- 전체 그래프가 모든 문서를 보여주지만 오히려 사용자의 인지 복잡성을 높일 위험

## 9. 중요 파일

| 경로 | 상태 | 용도 |
|---|---|---|
| `SESSION_HANDOFF.md` | 활성 정본 | 현재 상태, 중단점, 첫 다음 행동 |
| `reports/2026-07-22_프로젝트_의도_통합_요구사항_기준서.md` | 활성 기준 | 현재 요구사항, 수용 기준, 병합된 세 분석 |
| `docs/build/MASTER_BUILD_PLAN.md` | 활성 계획 | Stage 00~09와 Stage 01.5 순서, 공통 게이트, 모든 단계의 네 가지 확인 규칙 |
| `docs/build/stage-00-project-kernel.md` | 다음 단계 계획 | Stage 00 범위, 제외 사항, 사용자 결정 |
| `docs/build/stage-01-5-obsidian-document-visibility.md` | 승인된 후속 단계 계획 | Stage 01 뒤 전체 활성 문서·규칙·연결을 보여주는 Obsidian 검토 환경 |
| `docs/build/stage-01-information-architecture.md` ~ `stage-09-domain-integration.md` | 후속 단계 계획 | 현재 구현 지시가 아니며 해당 단계에서만 읽음 |
| `reports/2026-07-22_codex_프로젝트_의도_역추론_보고서.md` | 분석 근거 | 현재 요구사항 기준으로 선택된 역추론 보고서 |
| `reports/2026-07-22_claude_프로젝트_의도_역추론_보고서.md` | 분석 근거 | 인지 복잡성·승인 실패·원문 보존 분석의 출처 |
| `reports/2026-07-22_claude_역추론보고서_교차검증_점수.md` | 시점 분석 | 두 보고서 교차검증 자료. 현재 요구사항 정본은 아님 |
| `reports/2026-07-22_codex_문서_기반_데이터_구축_체계_제안_보고서.md` | 시점 분석·구현순서 비권위 | 문서 기반 데이터 체계 조사. 구현 순서는 마스터 계획이 우선 |
| `backup/origin`, `backup/refactorying_snapshot_1`, `backup/refactorying_snapshot_2`, `backup/refactorying_snapshot_3` | 역사·읽기 전용 | 근거 확인에만 사용. 수정·이관·활성 의존 금지 |

Stage 01.5 설계 묶음은 커밋 `783a1ff`에 보존됐다. 이후 Git 추적·변경·원격 상태는 동적 사실이므로 다음 세션에서 다시 조회하고, 커밋 존재만으로 push 완료를 추정하지 않는다.

## 10. 정확한 재개 체크포인트

### 완료된 지점

- 현재 의도와 요구사항 통합 완료
- 00~09단계와 Stage 01.5 설계·계획 문서 작성 완료
- 마스터 계획에서 통합 요구사항 기준서로 연결 완료
- 모든 단계에 적용할 네 가지 확인·증거·차단 규칙 추가
- Obsidian 문서 가시화·검토 환경을 Stage 01.5로 배치하고 요구사항·인접 단계 연결
- 현재 세션 핸드오프 갱신

### 첫 미착수 행동

사용자가 **Stage 00 구현 착수**를 요청하면, 구현 전에 `docs/build/stage-00-project-kernel.md`의 사용자 결정과 이 핸드오프 §8.1을 중복 없이 정리해 질문한다.

사용자가 구현을 요청하지 않았다면 질문이나 파일 생성을 선행하지 않고 현재 계획 상태를 설명한다.

### Stage 00 착수 후 순서

1. Stage 00에만 필요한 사용자 결정을 확인한다.
2. 답변을 Stage 00 계획 문서의 결정 기록에 반영한다.
3. 변경 파일, 구현 범위, 제외 범위를 보고한다.
4. Stage 00 기능만 구현한다.
5. 단계 검증을 한 번 수행한다.
6. 사용자 목적 정합성, 실제 기능 작동, 미래 단계 선행 유입, 과거 실패 재발 여부를 각각 근거와 함께 판정한다.
7. 실제 결과, 미구현, 미검증, 위험을 구분해 보고한다.
8. 네 항목이 모두 통과하고 사용자가 결과와 Stage 01 착수를 승인할 때까지 다음 단계로 이동하지 않는다.

## 11. 하지 않을 것

- 영상 제작 워크플로 구현 또는 복귀
- Stage 00에서 파일 스키마·지식 수명주기·검색·RAG 구현
- 실제 지식 코퍼스 대량 구축이나 과거 자료 전량 이관
- 필요 증명 없는 SQLite·벡터 DB·그래프 DB와 사용자 승인 없는 Obsidian 플러그인 도입
- Obsidian을 정본이나 필수 실행 의존성으로 사용
- `backup/`, `inputs/`, `outputs/`의 수정 또는 무단 탐색
- 사용자 승인 없는 삭제·이동·설치·commit·push·외부 변경
- 보고서나 역사 핸드오프를 현재 실행 지시로 사용

## 12. 다음 세션 시작 프롬프트

> 대화 시작 규칙에 따라 각 역사 프로젝트의 `PROJECT_RULES.md`를 끝까지 읽은 뒤 `SESSION_HANDOFF.md`, 통합 요구사항 기준서, 마스터 계획을 읽어라. 현재 요구사항 통합과 Stage 00~09 및 Stage 01.5 계획 작성은 완료됐지만 구현은 시작하지 않았다. 사용자가 Stage 00 착수를 요청한 경우에만 Stage 00 계획서를 읽고, 관련 미확정 항목을 먼저 질문하라. Stage 01에서 문서 책임과 권위를 확정한 뒤 Stage 01.5에서 Obsidian 검토 환경을 구축하고, Stage 02부터 사용자가 전체 문서 지도와 단계별 연결을 보며 진행한다. 최종 형태를 앞당겨 구현하거나 이후 단계 기능을 현재 완료 조건으로 사용하지 마라. 모든 단계 종료 시 마스터 계획 §6.3의 네 가지 확인을 근거와 함께 모두 통과하기 전에는 완료 또는 다음 단계 착수를 요청하지 마라.
