# 세션 핸드오프

- 갱신일: 2026-07-23
- 역할: 채팅 기억 없이 현재 검증 상태와 첫 다음 행동을 재구성하는 단일 활성 상태 정본
- 현재 단계: Stage 01 정보·문서 책임 구조 — 착수 승인됨, 구현 전 사용자 결정 확인 대기
- 이전 단계: Stage 00 실패 지식 보존 보완 96/100 `완료 준비` — 검증된 31개 경계 커밋 완료
- 백업 정책: 별도 복제 없음. 활성 프로젝트는 Git 이력을 복구 근거로 사용한다.

## 1. 시작 순서와 권위

새 대화·세션을 시작할 때 다음 세 파일을 한 번만 끝까지 읽는다.

1. `AGENTS.md`
2. `PROJECT_RULES.md`
3. `SESSION_HANDOFF.md`

매 메시지마다 반복하지 않는다. 규칙이나 현재 상태가 변경됐다는 알림이 있을 때만 다시 읽는다. 이후에는 `AGENTS.md`의 작업 행동 표에서 현재 작업과 일치하는 `rules/*.md`만 읽고, 정확한 단계 계획이나 작업 자료를 선택한다.

권위 순서는 사용자의 최신 명시적 지시 → `PROJECT_RULES.md` → 이 핸드오프 → 현재 단계 계획서다. 요구사항의 정본은 `reports/2026-07-22_프로젝트_의도_통합_요구사항_기준서.md`, 구축 순서의 정본은 `docs/build/MASTER_BUILD_PLAN.md`다.

## 2. 현재 목표와 핵심 경계

현재 목표는 작업 결과·실패·판단·경험을 출처와 함께 선별 축적하고 다음 작업에 필요한 지식만 다시 사용하는 **데이터 기반 지식 운영 기반**이다. 영상 제작 기능은 기반 검증 후 별도 승인을 받아 연결할 후속 영역이다.

- 활성 프로젝트: 루트와 `docs/build/`, `reports/`의 현재 문서. `backup/` 없이 시작·재개해야 한다.
- 역사 증거: `backup/`. 사용자 승인 목적에 맞게 좁게 읽을 수 있지만 수정하거나 실행에 의존하지 않는다.
- 보호 데이터: 경로 구간이 `inputs` 또는 `outputs`인 자료. 사용자가 정확한 대상과 목적을 지정하지 않으면 열거·열람·요약하지 않는다.
- 현재 상태 소유자: 이 `SESSION_HANDOFF.md` 하나다. 동적 Git 상태는 필요할 때 다시 조회한다.

## 3. 이번 Stage 00 입력과 사용자 결정

- 입력: `docs/build/stage-00-project-kernel.md`, 통합 요구사항 기준서, 마스터 계획, 활성 루트 상태.
- 역사 비교: 네 `backup/` 버전의 `PROJECT_RULES.md`와 선별된 `AGENTS.md`, 부트스트랩·인덱스·README·문서 라우팅 자료.
- 사용자 결정: 시작 문서는 세션당 한 번 최대 3개, 상시 규칙과 작업 규칙은 분리 저장·선택, README는 사용자 중심, 한 화면 개요 사용, 단계 계획서가 단계별 승인을 소유하고 이 핸드오프는 활성 승인만 요약한다. 각 단계에서 해결된 실패는 별도 `failures/` 폴더의 단계 독립 지식으로 보존하고 기록 규칙을 적용한다.
- 세부 결정과 채택·제외 근거: `docs/build/stage-00-project-kernel.md#9-사용자-결정-기록`.

## 4. 구현된 내용

| 경로 | 상태 | 역할 |
|---|---|---|
| `AGENTS.md` | 활성·신규 | 세션 시작 3문서 순서와 작업 행동별 규칙 직접 라우팅 |
| `PROJECT_RULES.md` | 활성·신규 | 세션 시작 시 한 번 읽는 최소 상시 규칙과 규칙 분류 계약 |
| `rules/stage-work.md` | 활성·신규 | 단계 구현·검토·검증·종료 시 읽는 작업 규칙 |
| `rules/failure-records.md` | 활성·신규 | 실패 기록·원인별 중복 병합·단계 종료 정리 규칙 |
| `rules/document-work.md` | 활성·신규 | 유지 문서 생성·수정·검증 시 읽는 작업 규칙 |
| `rules/history-review.md` | 활성·신규 | `backup/`·역사 보고서 조사 시 읽는 작업 규칙 |
| `rules/version-control.md` | 활성·신규 | Git 쓰기·복구·백업 결정 시 읽는 작업 규칙 |
| `rules/user-data-work.md` | 활성·신규 | 사용자가 정확히 지정한 보호 데이터 작업 전에 읽는 규칙 |
| `README.md` | 활성·신규 | 사용자가 목적, 문서 역할, 기본 폴더 경계, 현재 상태 정본을 한 화면에서 확인하는 개요 |
| `failures/README.md` | 활성·신규 | 해결된 실패 지식의 역할·경계와 링크 전용 색인 |
| `failures/*.md` 8개 | 활성·신규 | 단계와 무관하게 재사용하는 원인별 증상·해결·검증·재발 방지 정본 |
| `SESSION_HANDOFF.md` | 활성·갱신 | 현재 단계, 검증, 위험, 첫 다음 행동의 단일 정본 |
| `docs/build/stage-00-project-kernel.md` | 활성·갱신 | Stage 00 사용자 결정, 검증, 실패 지식 보완, 96/100 자체 검토와 잔여 감점 |
| `docs/build/MASTER_BUILD_PLAN.md` | 활성·갱신 | 모든 단계의 네 가지 확인, 실패 지식 보존, 최종 자체 검토·100점 평가·차단 규칙 |
| `docs/build/stage-01-*.md` ~ `stage-09-*.md` | 활성·갱신 | 각 단계의 실제 점수를 기록할 최종 게이트 위치와 분리된 완료 승인 조건 |
| `reports/2026-07-22_프로젝트_의도_통합_요구사항_기준서.md` | 활성·변경 없음 | 현재 요구사항과 수용 기준 |

데이터 형식, 코드, 도구, 스키마, 검색, 지식 수명주기, 자동화, Obsidian 구성은 구현하지 않았다.

## 5. 검증 상태

2026-07-23 규칙 분류 보정 후 Stage 00 통합 검증이 통과했다.

- 시작 분리: `PROJECT_RULES.md`만 상시 규칙으로 세션 시작 시 읽고 작업 팩은 시작 목록에서 제외됨.
- 작업 선택: `AGENTS.md`의 여섯 작업 행동과 여섯 실제 `rules/*.md` 파일이 정확히 대응함.
- 복합 작업: 일치하는 팩의 합집합을 만들고 각 팩을 한 번만 읽으며 미선택 팩은 전량 로드하지 않도록 명시됨.
- 중복·메타데이터: 상시·작업 규칙의 동일 문장 중복 0건, 모든 팩에 목적·읽기 시점·소유자·권위 존재.
- 구조 검증: 최종 31개 변경의 완성 게이트 35개 단언에 규칙 라우팅, UTF-8·NUL·링크·공백·diff, `backup/` 무변경 검사를 포함해 통과.
- 네 가지 확인: 최신 사용자 지시 정합성, 실제 규칙 선택, 자동 resolver 선행 유입 방지, 대형 라우터·중복 규칙 재발 방지 모두 통과. 상세 근거는 `docs/build/stage-00-project-kernel.md#101-네-가지-확인-판정`.
- 최종 자체 검토: 96/100. 마스터의 오래된 상태·폐기 링크, 단계 완료·다음 착수의 묶음 승인, 단계별 점수 기록 위치 누락을 발견해 수정했다.
- Stage 00 자체 검토 당시 최종 게이트 통합 검증: 관련 활성 문서 21개, 완료 게이트 단언 19개, UTF-8·NUL·링크·공백·diff·`backup/` 무변경 검사 통과.
- 최종 판정: `완료 준비`, 96/100. 독립 새 에이전트 세션 재개 검증 부재 2점과 수동 라우팅 확장·자동 검사 부재 2점은 사용자가 Stage 00 잔여 위험으로 수용했다.
- 사용자 수용 반영 뒤 첫 커밋 전 재검증은 검사기 가정 오류가 3회 연속 발생해 중단됐으나, 다음 사용자 실행 요청에서 보정·전체 재검증해 아래 성공 근거로 해소했다.
- 다음 사용자 실행 요청에서 세 가정을 보정한 기존 21개 파일 게이트를 처음부터 다시 실행해 종료 코드 0으로 통과했고 연속 실패 횟수를 0으로 재설정했다.
- 실패 지식 보완 예비 검증: 원인별 사례 8개와 색인 1개의 필수 절, 제목 중복, 엄격한 UTF-8, NUL, 후행 공백, 로컬 링크 통과.
- 실패 지식 보완 전체 통합 게이트: 변경 31개, 단언 35개에서 엄격한 UTF-8, NUL 0, 공백, 로컬 링크, 6개 규칙 라우팅, 8개 실패 사례 구조·중복, 단계 완료 차단, Stage 05 정본 중복 방지, 모든 단계 점수 기록 위치, 보호 경계, `git diff --check` 통과.
- 최초 캐시 패치 검사에서 신규 파일 12개의 EOF 추가 빈 줄을 발견했다. 스테이징 전 `git diff --check`가 미추적 파일을 포함하지 않은 원인을 `failures/windows-validation-command-assumptions.md`에 추가하고 EOF를 보정한 뒤, 스테이징 31개·미스테이징 0개·보호 경로 0개·`git diff --cached --check` 통과.
- 보완 후 자체 점수: 96/100 `완료 준비`. 독립 새 에이전트 세션 검증 2점과 수동 라우팅 확장·자동 검사 부재 2점은 기존 사용자 승인 잔여 위험으로 유지한다.
- Stage 00 커밋 검증: 커밋 `8dc034e`, 포함 경로 31개, 보호 경로 0개, 커밋 직후 작업트리 깨끗함을 확인했다. 이는 Stage 00 구현 경계의 고정 근거이며 현재 Git 상태는 필요할 때 다시 조회한다.

검증은 사용자 승인을 대신하지 않는다. 2026-07-23 사용자가 Stage 00 결과와 완료를 승인했고 Stage 01 착수를 별도로 승인했다.

## 6. 활성 실패 원장

현재 미해결 실패와 활성 연속 실패 횟수는 없다. 직전 커밋 전 완성 게이트 검사기의 최고 연속 실패 횟수는 3이었으나, 보정된 전체 게이트 성공으로 0이 됐다.

해결된 실패의 원인·시도·해결·검증·재발 방지 지식은 [failures/README.md](failures/README.md)와 연결된 원인별 사례가 소유한다. 새 활성 실패가 생기면 해결 전까지 이 절에 목표, 시도, 확인 원인, 연속 횟수, 재개 조건을 기록한다.

## 7. 활성 차단과 위험

- Stage 01 착수는 승인됐지만, 아래 정보 구조 결정을 사용자에게 확인하기 전에는 구현하지 않는다.
- 현재 Stage 01 초안은 `SESSION_HANDOFF.md`를 파생 문서 예시에 두지만 실제로는 현재 상태의 단일 정본이다. 권위 역할과 문서 유형을 한 축으로 강제하면 충돌하므로 두 축 분류로 보정할지 사용자 결정이 필요하다.
- 활성 문서의 유형별 기본 언어, 에이전트용·사용자용 문서의 물리 분리 수준, 시점 보고서 보존·대체 정책, 최소 메타데이터 방식, 정보 구조 계약 파일명, `failures/*.md`의 문서 유형을 확정해야 한다.
- Stage 00 자체 점수는 96/100이며 두 감점은 사용자 수용을 받은 승인된 잔여 위험이다.
- 직접 라우팅 표가 작업 유형 증가에 따라 커지면 다시 인지 복잡성이 생길 수 있다. 반복되는 실제 작업이 확인될 때만 새 팩을 추가한다.
- README가 현재 상태 값을 복제하면 핸드오프와 두 개의 상태 소유자가 생긴다. README는 상태 링크만 유지한다.
- `backup/`에서 도출한 도메인·런타임·스키마 규칙을 상시 규칙으로 승격하지 않는다.

## 8. 정확한 재개 체크포인트

완료한 작업:

- Stage 00 정책 결정 확정
- 네 역사 버전의 기본 구조 비교
- 최소 시작 라우터·상시 규칙·사용자 개요 작성
- 핸드오프 축약과 문서 역할 분리
- 상시 규칙과 여섯 작업 규칙 팩 분리 및 직접 라우팅 구현
- 규칙 선택·중복·메타데이터·보호 경계 통합 검증 통과
- 모든 단계 공통 최종 자체 검토·점수 게이트와 단계별 기록 위치 추가
- 자체 검토에서 발견한 오래된 마스터 상태·승인 묶음·기록 위치 누락 수정
- Stage 00 자체 점수 96/100, `완료 준비` 판정
- 두 잔여 감점의 사용자 수용과 Stage 00 완료 승인
- Stage 01 별도 착수 승인
- 보정된 기존 21개 파일 완성 게이트 전체 성공과 연속 실패 횟수 0 재설정
- 단계 독립 `failures/` 폴더, 링크 색인, 원인별 사례 8개 작성
- 실패 기록 작업 규칙과 단계 완료 보존 게이트 추가
- 검증된 31개 Stage 00 경계를 커밋 `8dc034e`로 기록하고 포함 경로·보호 경계·깨끗한 작업트리 검증

첫 미착수 행동:

1. Stage 01의 여섯 미확정 결정과 권장안·영향을 사용자에게 보고하고 승인받는다.
2. 답변을 `docs/build/stage-01-information-architecture.md`의 결정 기록에 반영한다.
3. 변경 파일과 제외 범위를 보고한 뒤 Stage 01 정보·문서 책임 계약만 구현한다.

## 9. 다음 세션 시작 프롬프트

> `AGENTS.md`, `PROJECT_RULES.md`, `SESSION_HANDOFF.md`를 새 세션 시작 시 한 번 읽어라. 그 뒤 현재 행동과 일치하는 `rules/*.md`만 `AGENTS.md` 표에서 선택해 읽어라. Stage 00은 96/100 `완료 준비`로 검증된 31개 경계가 커밋 `8dc034e`에 기록됐다. 현재는 Stage 01 착수 승인 후 여섯 정보 구조 결정의 사용자 확인 대기다. 권위 역할·문서 유형의 두 축 분류, 언어, 물리 배치, 보고서 보존, 최소 메타데이터, 계약 파일명과 실패 사례 유형을 승인받기 전에는 구현하지 마라.
