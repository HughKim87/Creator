interface:
  display_name: "Prepare YouTube Upload"
  short_description: "업로드용 네 파일만 남기고 기술 자료는 별도 보관"
  default_prompt: "Use $prepare-youtube-upload to validate the approved assets, create the manual upload guide, and leave only the four user-facing upload files in outputs."
