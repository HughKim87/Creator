interface:
  display_name: "Chrome 프로필 연결"
  short_description: "전달받은 Chrome 프로필과 대상 탭을 정확히 연결합니다."
  default_prompt: "Use $connect-chrome-profile with the caller-provided profile_directory and target_origin before any browser selection, then return only the verified exact Chrome binding and tab."
