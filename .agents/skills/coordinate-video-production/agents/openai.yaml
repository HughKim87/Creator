interface:
  display_name: "Coordinate Video Production"
  short_description: "기본 프로필 자동 해석부터 최종 네 파일까지 영상 제작 조정"
  default_prompt: "Use $coordinate-video-production to resolve an explicit browser profile or the worktree-local runtime default into the video job, then continue the verified workflow through a four-file manual upload output."
