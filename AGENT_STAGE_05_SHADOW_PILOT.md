# 단계 05 작업 지시서: 골든·실영상 병행 파일럿

## 1. 단계 목적

같은 입력을 구체계와 신규 구조에서 독립 실행하여 기술 결과와 사람의 A/V 품질을
비교한다. 신규 구조의 테스트 통과만으로 전환하지 않는다.

## 2. 시작 게이트

- 단계 04 수직 스모크가 3회 연속 통과했다.
- 신규 구조와 구체계가 서로 다른 DB·출력 경로를 사용한다.
- 구체계 보존 SHA 또는 태그가 고정됐다.
- 대표 실제 영상 1개와 원본 해시가 선정됐다.
- 사용자가 허용한 산출물 종류와 MP4 생성 여부가 명시됐다.
- 사용자에게 최종 A/V 검토가 필요함을 알렸다.
- 다음 두 비교 모드 중 하나를 구현 전에 동결했다.
  - `direct_shadow`: 사용자 승인으로 만든 legacy worktree 스모크가 통과함
  - `frozen_legacy_baseline`: legacy 실행이 불가능하여 Stage 00의 동결된 골든 산출물·
    매니페스트를 비교 기준으로 쓰고, 사용자가 `legacy_execution_waived` 위험을 승인함

## 3. 역할과 권한

### 파일럿 실행 에이전트

- 승인된 외부 파일럿 작업 공간만 쓸 수 있다.
- 원본 입력은 읽기 전용으로 취급한다.
- 구/신규 출력 경로를 완전히 분리한다.

### 기술 비교 에이전트

- 구/신규 결과와 DB를 읽기 전용 비교한다.
- 결과 수정이나 재생성을 하지 않는다.

### 리뷰 렌더 조정 에이전트

- 사용자에게 Premiere 수동 import/export와 승인된 자동 렌더 중 선택지를 제시한다.
- 선택된 렌더 설정과 구·신규 산출물 경로를 동결한다.
- 에이전트가 사람 A/V 승인 결정을 대신하지 않는다.

### 레드팀 에이전트

- 승인 우회, 출력 충돌, 입력 변조, 재개 실패를 공격한다.

### 사용자

- 기술 비교 자료를 받고 실제 A/V 품질을 승인·조건부 승인·거부한다.

## 4. 파일럿 데이터

### 자동 골든 픽스처

- 짧고 결정적이며 저작권·개인정보 문제가 없어야 한다.
- 가능하면 테스트 중 합성한다.
- 오디오, 비디오, 컷어웨이, 프레임 경계를 포함한다.
- Git에는 생성 코드와 기대 메타데이터만 둔다.

### 대표 실제 영상

- 실제 해상도, FPS, 오디오 채널, 길이를 대표한다.
- Git, CI, 외부 AI 서비스에 업로드하지 않는다.
- 원본은 읽기 전용이며 논리 ID, SHA-256, 길이, 코덱만 기록한다.

## 5. 출력 분리

```text
pilot/<pilot_id>/
├─ legacy/<run_id>/
│  └─ review/
├─ new/<run_id>/
│  └─ review/
└─ comparison/<run_id>/
```

- 구/신규가 같은 `CURRENT`, DB, 임시 폴더, 산출물을 사용하지 않는다.
- 신규 실행마다 새 `generation_id`를 사용한다.
- 구체계는 `backup/` 중첩 위치가 아니라 보존 SHA의 별도 worktree에서 실행한다.

## 6. 세부 작업

### 작업 05-1: 환경과 입력 고정

각 실행에 다음을 기록한다.

- 구체계·신규 구조 커밋
- Python, 잠금 의존성, FFmpeg/FFprobe 버전
- OS와 작업 공간
- 입력 논리 ID와 해시
- 실행 명령과 실제 종료 코드
- 시작·종료 시각

### 작업 05-2: dry-run과 무결성 검사

1. 신규 `doctor`와 full check 실행
2. 원본 해시와 파일 속성 기록
3. 승인된 산출물·경로 확인
4. 예상 쓰기 파일 목록 출력
5. 구/신규 출력 충돌 0건 확인

dry-run과 실제 실행이 다른 경로를 쓰면 중단한다.

### 작업 05-3: 독립 실행

- 자동 골든 흐름은 캐시·DB·이전 출력을 재사용하지 않는 같은 깨끗한 조건에서 3회
  연속 실행한다.
- `direct_shadow`에서는 대표 실제 영상을 구체계와 신규 구조에서 각각 최소 1회 독립
  실행한다.
- `frozen_legacy_baseline`에서는 해시가 동결된 legacy 실제 영상 리뷰 artifact·XML·
  매니페스트를 비교 기준으로 쓰고 신규 구조만 최소 1회 실제 실행한다. legacy 실행을
  수행한 것처럼 보고하지 않으며 결과 상태를 `pilot_limited_legacy_baseline`으로 둔다.
- 이전 출력·DB·캐시를 재사용했는지 실행별로 기록한다.
- 실패하면 같은 명령을 무조건 반복하지 않는다.
- 실패 이벤트와 증거를 기록하고 마지막 유효 이벤트에서 재개 가능한지 확인한다.
- 실제 실행 전후 원본 해시를 다시 검사한다.

### 작업 05-4: 기술 비교

최소 비교 항목:

- 입력 해시와 source identity
- 전체 길이, FPS, 시간 기준
- 오디오 샘플레이트와 채널
- 컷 수와 각 컷의 소스·타임라인 프레임
- 누락·중복 구간
- 컷어웨이 트랙과 원음 관계
- 동기화 오프셋
- 검은 프레임, 빈 오디오
- XML 의미 구조
- 산출물 매니페스트
- 세대·부모 계보

차이를 `critical`, `major`, `minor`, `expected`로 분류한다. `expected`는 사전 근거와
승인자가 있어야 한다.

### 작업 05-5: 사람 A/V 검토 자료

에이전트는 다음만 준비하고 승인 결정을 대신하지 않는다.

- 구체계와 신규 결과
- 차이가 큰 타임코드
- 기술 비교 요약
- 알려진 제한

Premiere XML은 재생 가능한 영상이 아니므로 다음 리뷰 렌더 경로를 반드시 완료한다.

1. 사용자가 Premiere 수동 import/export 또는 사전 승인된 자동 렌더 경로를 선택한다.
2. 구·신규에 같은 시퀀스, 코덱, FPS, 해상도, 오디오 채널·샘플레이트·음량 설정을
   적용한다.
3. 리뷰 MP4는 각 실행의 `review/`에 생성하고 다른 실행 결과를 덮어쓰지 않는다.
4. 각 리뷰 파일을 artifact ID, 해시, 크기, 생성 설정, 부모 XML과 함께 등록한다.
5. 사람 A/V 승인은 XML이나 경로가 아니라 검토한 리뷰 MP4의 artifact ID와 해시를
   직접 참조한다.

리뷰 렌더가 없으면 기술 비교만 완료된 것이다. 외부 렌더 환경 문제는 lifecycle을
`blocked_external`로 두고 reason을 `blocked_render`로 기록하며, 사용자 결정 대기는
`waiting_user`로 둔다. 단계 05를 완료하거나 전환으로 진행하지 않는다.

사용자는 동기화, 장면 누락, 중복·검은 프레임, 오디오 단절·음량, 컷어웨이와 원음,
편집 의도를 평가한다. 결과는 DB에 별도 사람 승인 이벤트로 기록한다.

### 작업 05-6: 실패 기록

모든 실패에 `failure_id`, 지문, 재현 입력, 직접·근본 원인, 영향 범위, 회귀 테스트,
수정 식별자, 재검증 결과를 기록한다. 같은 명령을 변경 없이 반복한 것은 해결 시도로
세지 않는다.

### 작업 05-7: 전환 전 최소 백업·복구 시험

Stage 07의 정기 Restic 운영보다 먼저, 이 단계의 승인된 로컬 저장소에서 최소 복구
가능성을 증명한다.

1. 애플리케이션 커밋·버전, DB schema version, 마지막 migration을 기록한다.
2. SQLite backup API로 일관된 DB 사본을 만든다.
3. 입력과 승인된 산출물의 파일 목록·해시 매니페스트 및 승인된 로컬 백업 사본을 만든다.
4. 완전히 빈 한글·공백 임시 경로에 코드, DB, 파일을 복원한다.
5. 코드-스키마 호환성, `state verify`, 입력 해시, artifact reconciliation과 작은
   스모크를 실행한다.
6. 실제 종료 코드, 누락 파일, 소요시간을 기록한다.

이 시험은 정기 예약, 오프사이트, 보존 정책을 도입하지 않는다. 그것들은 Stage 07의
운영화 범위다.

## 7. 레드팀 시나리오

- 승인 이벤트 없이 다음 단계 전이
- 구/신규가 같은 출력 파일을 쓰려는 시도
- 실행 도중 원본 파일 교체
- 외부 명령 실패 후 최종 코드 0 반환 시도
- 실패 세대의 산출물 승격
- 생성 상태 문서 변조 후 재개
- DB 스냅샷에서 재개
- 한글·공백 경로 파일럿
- 리뷰 MP4와 승인 이벤트의 artifact ID·해시 불일치
- 복원 코드와 DB schema version 불일치

## 8. 완료 기준: 파일럿 통과

- 입력 변경 0건
- Critical 차이 0건
- 설명되지 않은 Major 퇴행 0건
- 승인 없는 전이 0건
- 등록되지 않은 산출물 0건
- 자동 골든 흐름이 깨끗한 조건에서 3회 연속 통과
- 대표 실제 영상이 다음 중 선택된 모드의 기준을 충족하고 기술 비교 통과
  - `direct_shadow`: 구·신규 각각 최소 1회 독립 실행
  - `frozen_legacy_baseline`: 검증된 동결 legacy artifact와 신규 최소 1회 독립 실행,
    `legacy_execution_waived`, `pilot_limited_legacy_baseline` 표시
- 실패 주입 시 올바른 비정상 종료
- 기술 검사 통과
- 사용자 A/V 승인
- 롤백 재실행 시험 성공
- 코드·DB·입력·승인 산출물의 빈 경로 최소 복구 시험 성공

승인 판정은 다음으로 고정한다.

- `approved`: 단계 완료 가능
- `conditional`: 조건, 책임자, 기한을 기록하고 `waiting_user`; 해결·재검토 전 완료 금지
- `deferred`: 실행 상태를 보존한 `waiting_user`
- `rejected`: 단계 실패와 결함 증거 기록
- 사용자 무응답: `waiting_user`

승인으로 추정하거나 자동 통과시키지 않는다.

## 9. 중단 조건

- 사용자 품질 거부
- 원본 해시 변경
- 같은 근본 원인의 실패가 세 번 반복
- 구체계와 신규 결과 차이를 분류할 수 없음
- 신규 구조가 구체계 경로에 runtime 의존
- 복구·재개 시험 실패

## 10. 롤백

1. 신규 실행을 `aborted`로 기록한다.
2. 실패한 파일럿 DB 전체, artifact 매니페스트, 실행 로그를 읽기 전용 증거 경로에
   동결하고 해시한다.
3. 실패 산출물을 삭제하지 않고 별도 세대로 격리한다.
4. 입력 해시를 재확인한다.
5. 재시도는 실행 전 스냅샷을 **새 DB 경로**에 복제하고 새 `run_id`로 시작한다. 실패
   증거가 든 DB를 제자리에서 되감지 않는다.
6. 라이브 DB 복원이 꼭 필요하면 불변 실패 보고서를 먼저 export하고 사용자에게 DB
   덮어쓰기 승인을 받은 뒤 복원한다. 복원된 DB에는 `rollback_performed` 이벤트를 다시
   기록한다.
7. 보존 SHA의 구체계를 승인된 별도 경로에서 실행한다.
8. 회귀 테스트와 근본 원인 수정 전 파일럿을 재시작하지 않는다.
