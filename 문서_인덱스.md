# 문서 인덱스 — 김실버유튜브

- 최종 갱신: 2026-07-08
- 목적: 세션 시작 시 필요한 문서만 고르는 단일 진입점.
- 현재 상태: `workspace/`와 기존 `temp/` 내용은 삭제됨. 과거 원본·산출물·아카이브를 현재 기준으로 삼지 않는다.

## 0. 기본 로드

모든 세션에서 아래 3개만 먼저 읽는다.

1. `PROJECT_RULES.md`
2. `SESSION_HANDOFF.md`
3. `문서_인덱스.md`

## 1. 현재 작업

| 항목 | 현재 상태 |
|---|---|
| 진행 영상 | 없음 |
| 원본 영상·자막 | 없음 |
| 현재 산출물 | 없음 |
| 현재 목표 | 남아 있는 기준 문서·스킬·도구 분석 및 프로젝트 재정리 |
| 다음 작업 | 단계별 작업방식 개선·최적화 계획 확정 후 한 단계씩 보강 |
| 편집 작업 조건 | 새 원본 제공 + 상위 단계 기준 확정 + 필요한 단계 개선 완료 후 |

## 2. 현재 기준 문서

| 문서 | 역할 |
|---|---|
| `PROJECT_RULES.md` | 전역 규칙 |
| `SESSION_HANDOFF.md` | 현재 세션 상태 |
| `00_공통_작업원칙.md` | 원본 보호, 파일 생성/삭제, 의도 확인 |
| `01_유튜브_제작_워크플로우.md` | 제작 단계와 단계별 완료 기준 |
| `기획_리서치/기획단계_규격_2026-07-06.md` | 5단계 대사 기반 기획 규격 |
| `기획_리서치/프로젝트_운영문제_진단_및_개선안_2026-07-07.md` | 현재 재정리 상태와 운영 진단 |
| `skills/README.md` | 스킬 목록 |
| `tools/README.md` | 도구 상태 |

## 3. 현재 폴더 상태

| 위치 | 상태 |
|---|---|
| `skills/` | 유지. 운영 스킬 5종 존재 |
| `tools/` | 유지. FFmpeg, synccheck, remux 도구 존재 |
| `기획_리서치/` | 현재 기준 문서 2개만 존재 |
| `reports/` | 유지. 현재 작업 기준으로는 기본 로드하지 않음 |
| `workspace/` | 없음. 새 원본이 생기면 다시 만든다 |
| `temp/` | 현재 없음. 필요 시 임시 작업 폴더로만 생성 |

## 4. 작업별 추가 로드

| 상황 | 추가로 읽을 문서 |
|---|---|
| 전체 워크플로우 확인 | `01_유튜브_제작_워크플로우.md` |
| 5단계 기획 규격 확인 | `기획_리서치/기획단계_규격_2026-07-06.md` |
| 스킬 상태 확인 | `skills/README.md`, 필요한 `skills/*/SKILL.md` |
| 도구 상태 확인 | `tools/README.md` |
| 새 원본 영상 작업 시작 | 먼저 `workspace/inputs/`를 만들고 원본을 배치한 뒤 해당 단계 문서만 로드 |

## 5. 현재 무효 참조

아래 경로는 현재 존재하지 않으므로 기준 입력으로 삼지 않는다.

| 경로 | 상태 |
|---|---|
| `workspace/inputs/` | 삭제됨 |
| `workspace/outputs/` | 삭제됨 |
| `temp/archive/2026-07-08/` | 삭제됨 |
| 과거 백룸 기획서·컷리스트·XML·러프컷 | 삭제됨 |
| 과거 5·6단계 개선 리서치 문서 | 삭제됨 |

## 6. 새 문서 생성 게이트

새 문서는 아래 질문에 모두 통과할 때만 만든다.

1. 기존 현재 기준 문서에 추가하면 안 되는가?
2. 기존 참고 문서의 섹션 추가로 충분하지 않은가?
3. 다음 세션에서 다시 찾을 만큼 비자명한 결정·근거·함정인가?
4. 새 문서가 대체할 기존 문서가 있는가?
5. 만들자마자 이 인덱스에 상태, 읽는 조건, 대체 관계를 등록할 수 있는가?

하나라도 아니면 새 문서를 만들지 않는다.
