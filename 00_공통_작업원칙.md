# 공통 작업 원칙

이 문서는 모든 유튜브 작업, 워크플로우, 스킬에 공통으로 적용되는 안전장치다.

## 1. 원본 보호

- 원본 영상, 원본 자막, 사용자가 직접 만든 파일은 절대 덮어쓰지 않는다.
- 변환, 추출, 정리, 편집 결과는 항상 새 파일로 저장한다.
- 원본 파일 이름을 바꾸거나 복사본으로 대체하지 않는다.
- 프리미어용 XML, EDL, 컷리스트는 원본 파일을 참조만 하도록 만든다.

## 2. 파일 생성과 삭제

- 원본과 생성 산출물은 Git에 올리지 않는 `workspace/inputs`, `workspace/outputs` 아래에 둔다.
- Git에 남겨야 하는 작은 문서형 결과물(보고서, 스킬, 사용 방법, 규칙 정리)은 목적에 맞는 프로젝트 폴더에 둔다.
- 임시 파일과 최종 파일을 같은 위치에 섞지 않는다.
- 삭제 요청이 있으면 삭제 대상 목록을 먼저 확인하고, 원본과 사용자가 제공한 파일은 제외한다.
- 내가 만든 산출물이라도 확신이 없으면 삭제하지 않는다.

## 3. 의도 확인

- 사용자의 의도가 불명확하면 작업을 진행하기 전에 먼저 확인한다.
- 같은 방향으로 두 번 이상 반박받으면 즉시 멈추고, 내가 이해한 의도를 한 문장으로 재확인한다.
- 사용자가 원하는 것이 `분석`, `기획`, `생성`, `수정`, `삭제` 중 무엇인지 먼저 구분한다.

## 4. 답변 방식

- 사용자가 화가 났거나 작업이 꼬였을 때는 장황하게 설명하지 않는다.
- 필요한 말만 한다: 현재 상태, 다음 작업, 사용자가 해야 할 일, 결과 파일 위치.
- 전문 용어를 쓸 때는 짧게 풀어서 설명한다.

## 5. 중복 방지

- 워크플로우는 작업 순서와 스킬 호출 조건만 설명한다.
- 스킬은 자기 작업의 실행 방법만 설명한다.
- 워크플로우에 스킬의 세부 절차를 복사하지 않는다.
- 스킬에 전체 유튜브 제작 흐름을 복사하지 않는다.
- 여러 스킬에 반복되는 규칙은 공통 기준 문서로 분리한다.
