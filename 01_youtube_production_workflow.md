# 영상 제작 워크플로우

반복 영상 제작 작업의 단계와 게이트를 정하는 지휘 문서다. 세부 실행은 각 스킬에 둔다.

## 운영 원칙

- 워크플로우는 자동 실행 목록이 아니라 단계별 게이트다.
- 산출물이 생겼다는 이유만으로 다음 단계로 넘어가지 않는다.
- 촬영 완료본은 자막 신뢰도 확인 뒤 5단계 영상 기획부터 실제 제작을 시작한다.
- 5단계 기획서 없이 6단계 분석으로 넘어가지 않는다.
- 6단계 산출물은 컷리스트, XML, 러프컷이 아니다.
- 7단계 편집 자료는 승인된 최신 컷 기준에서만 만든다.

## 제작 경로

```text
촬영 전 제작:
1 소재 조사 -> 2 채널 기획 -> 3 대본 구성 -> 촬영/녹화 -> 4 자막 정리 -> 5 대사 기반 기획 -> 6 영상 분석 -> 7 편집 자료 -> 8 최종 검수

촬영 완료본 제작:
원본 유입 확인 -> 4 자막 정리 또는 자막 신뢰도 확인 -> 5 대사 기반 기획 -> 6 영상 분석 -> 7 편집 자료 -> 8 최종 검수
```

촬영 완료본이라도 원본 자막이 없거나 신뢰도가 낮으면 4단계를 먼저 거친다.

## 단계

| 단계 | 목적 | 기준 입력 | 산출물 | 게이트 | 사용자 판단 |
|---:|---|---|---|---|---|
| 1 | 소재 조사 | 최신 게임/방송/검색/커뮤니티 근거 | 소재 후보표 | 수요, 최신성, 채널 적합도, 제외 이유 | 최종 소재 |
| 2 | 채널 기획 | 1단계 선택 소재 | 콘셉트, 핵심 주장, 제목/썸네일 방향 | 채널 관점과 클릭 이유 | 콘셉트 |
| 3 | 대본 구성 | 2단계 콘셉트 | 대본 구조, 주장 흐름, 장면 목적 | 주장과 근거 연결 | 톤과 주장 |
| 4 | 자막 정리 | 원본 영상, 원본 자막/전사 | 원본 보존 자막, 정리본, 신뢰도 | 원문 보존과 타임코드 신뢰도 표시 | 의미 변경 여부 |
| 5 | 대사 기반 기획 | 원본 자막, 5단계 규격 | 컨트롤링 아이디어, 대사 스파인, 6단계 브리프 | 메시지가 원본 대사로 추적됨 | 컨트롤링 아이디어 |
| 6 | 영상 분석 | 5단계 기획서 | 화면 검증표, 후보지도, 7단계 브리프 | 후보가 비트와 화면/자막/오디오 근거에 연결됨 | 대표 후보 |
| 7 | 편집 자료 | 승인된 6단계 후보지도 | 컷리스트, XML/EDL/CSV, 사용 방법 | 원본 참조, 오디오, 시퀀스, 컷 누락 검증 | 컷 흐름 |
| 8 | 최종 검수 | 완성본, 5단계 기획서, 7단계 자료 | 검수표, 수정 목록 | 기획 의도, 흐름, 오디오, 자막, 원본 보호 통과 | 업로드 판단 |

## 스킬

| 단계 | 스킬/규격 |
|---:|---|
| 1 | `game-trend-research` |
| 2 | `youtube-channel-planning` |
| 3 | `youtube-script-planning` |
| 4 | `subtitle-cleanup` |
| 5 | `dialogue-based-planning` + `planning_research/planning_stage_spec_2026-07-06.md` |
| 6 | `gameplay-video-analysis` + `video-watch` |
| 7 | `premiere-editing-export` |
| 8 | `final-video-review` |

## 입력과 산출물 경계

- 사용자가 넣은 원본은 `inputs/`에만 둔다.
- 입력을 분석·변환해 만든 파일은 종류와 무관하게 모두 `outputs/`에 둔다.
- 현재 영상의 작업 카드는 `outputs/SESSION_HANDOFF.md`에 기록한다.
- 특정 영상 전용 스크립트와 테스트는 관련 `outputs/<stage>/support/`에 함께 둔다.
- 여러 영상에 같은 입력 계약으로 재사용되는 규칙·도구·스킬만 공용 영역으로 승격한다.

새 원본이 들어왔을 때만 작업 카드를 만든다. 원본이 없으면 빈 작업 폴더를 만들지 않는다.

```markdown
## 작업 카드

- source_id:
- 원본 지문(경로·크기·수정 시각):
- 원본 영상:
- 원본 자막:
- 원본 보존 상태:
- 시작 경로: 촬영 전 / 촬영 완료본
- 시작 단계:
- 현재 단계:
- 기준 문서:
- 산출물 위치:
- 승인 상태:
- 차단 조건:
```

## 산출물 메타데이터

중요한 Markdown, CSV, XML, EDL, 러프컷 산출물에는 최소한 아래를 남긴다.

```markdown
## 산출물 상태

- 상태:
- source_id:
- source_start/source_end:
- asset_ids:
- 기준 입력:
- baseline_version:
- changed_ranges:
- 사용한 원본:
- 사용한 기준 문서:
- 생성 도구:
- 검증 수준:
- 다음 단계 사용 가능 여부:
- 사용자 승인 필요:
- 승인 상태:
- artifact_role: reusable_source_evidence / version_specific_render_evidence / current_deliverable
- supersedes:
- current_pointer:
- 사용 금지 조건:
```

검증 수준은 `PROJECT_RULES.md`의 `Generated`, `Parsed`, `Structure-validated`, `Tool-validated`, `App-validated`를 사용한다.

승인 상태는 아래 값만 쓴다.

| 상태 | 의미 |
|---|---|
| `draft` | AI 초안. 다음 단계 기준으로 사용 금지 |
| `needs_user_decision` | 사용자 선택 없이는 진행 불가 |
| `approved_for_next_stage` | 다음 단계 기준 입력으로 사용 가능 |
| `revision_requested` | 같은 단계에서 수정 필요 |
| `superseded` | 새 기준 파일로 대체됨 |
| `blocked` | 원본, 기준 파일, 도구, 권한 문제로 진행 불가 |

같은 원본 지문에 이미 `source_id`가 있으면 그 ID를 재사용한다. 변경 없는 재실행은 새 파일을
만들지 않는다. 새 버전은 기준 입력이나 판단이 바뀐 경우에만 만들고, 게이트 통과 후
`CURRENT.json`을 갱신한다. 이전 버전은 `superseded`와 정리 후보로 기록하되 자동 삭제하지 않는다.
보존하는 미디어는 원본 자산 목록이나 현재본 포인터 중 한 곳에 반드시 등록한다.

## 5단계와 6단계

5단계 영상 기획은 `planning_research/planning_stage_spec_2026-07-06.md`를 따른다.

- 컨트롤링 아이디어는 원본 대사 인용으로 뒷받침한다.
- 대사 스파인은 소리만 들어도 이야기여야 한다.
- 모든 비트는 컨트롤링 아이디어에 기여해야 한다.

6단계 영상 분석은 5단계 기획서의 비트를 화면으로 검증하는 작업이다.

- 신호나 리액션만으로 이야기를 새로 만들지 않는다.
- 컷리스트, XML, 러프컷을 만들지 않는다.
- 대사만 보고 화면 검증 완료로 처리하지 않는다.
- 화면·음성 근거는 원본의 `source_id`와 원본 시간/프레임으로 등록하고 기존 자산을 먼저 재사용한다.
- 편집본 시각으로 받은 검증 요청은 컷리스트를 통해 원본 시각으로 변환한다.
- 접촉 시트와 검토 HTML은 등록된 원본 자산을 참조하며 같은 장면을 다시 추출하지 않는다.

## 7단계 하위 작업

7단계는 편집 자료 생성을 담당하되, 내부 책임을 나눈다.

| 하위 단계 | 목적 | 입력 | 출력 |
|---|---|---|---|
| 7A | 후보범위 해석 | 승인된 6단계 후보지도, 보존/압축 기준 | 이야기 역할, 보존점, 압축·제거 가능 범위 |
| 7B | microbeat 설계·경계 검증 | 7A 해석, 원본, 자막, 오디오·화면 근거 | 유지·압축·제거 결정표, 실제 컷리스트, 경계 주의표 |
| 7C | 캘리브레이션 | 서로 다른 리듬을 대표하는 7B 컷리스트 | 대표 시퀀스 XML/CSV, 편집 문법, 사용 방법 |
| 7D | 전체 확장 | 사용자 피드백이 반영된 편집 문법, 전체 7B 컷리스트 | 전체 XML/CSV, 검증 자료, 사용 방법 |

6단계 후보범위는 탐색 범위이며 실제 컷이 아니다. 7A 이전에는 컷리스트를 만들지 않는다.
7B 경계 검증 전에는 XML, EDL, 러프컷을 만들지 않는다. 대표 리듬 검증 없이 전체 XML로
확장하지 않으며, 기술 검증 성공을 편집 감각 승인으로 간주하지 않는다.

전체 편집 품질은 `docs/EDITING_QUALITY_STANDARD.md`의 Q0~Q6 게이트를 적용한다.

- 새 버전은 이전 컷리스트와 원본 구간을 비교해 변경된 경계만 다시 검증한다.
- 편집본 캡처는 자막·효과·합성·렌더 오류 확인에만 쓰며 원본 내용 근거로 승격하지 않는다.
- 초반·중간·후반에서 각각 표본을 검증한다. 서로 다른 리듬 표본만 있고 후반 표본이 없으면 7D로 승격하지 않는다.
- 모든 비트에 유지·압축·제거 microbeat와 최장 컷 근거가 있어야 한다.
- 콘텐츠 기준 단일 시퀀스 CSV를 `tools/edit_quality_audit.py`로 감사한다. 멀티시퀀스 CSV는 중복되므로 입력 금지.
- 감사 결과가 `REVIEW_REQUIRED`이면 해당 비트를 재설계하거나 화면·문장·사건 근거가 있는 예외로 기록한다.
- 컷 수·평균 길이는 경고 신호이지 고정 목표가 아니다. 긴 컷을 없애기 위한 기계적 분할을 금지한다.

## 중단 조건

- 원본 파일이나 현재 기준 파일이 불명확하다.
- 게이트를 통과하지 않은 산출물로 다음 단계 작업을 요청받았다.
- 사용자가 원하는 결과물이 분석인지 생성인지 불명확하다.
- 같은 방향의 결과물이 두 번 이상 거절됐다.
- 프리미어 사용 방식이 XML, EDL, JSX 중 무엇인지 불명확하다.
- 기술 검증 성공을 콘텐츠 품질 승인처럼 말하게 된다.

## 종료 전 확인

- 최신 사용자 요청과 실제 산출물이 맞는가.
- 현재 단계 기준 문서와 스킬을 끝까지 읽었는가.
- 해당 단계 게이트를 통과했는가.
- AI가 확정하면 안 되는 판단을 사용자 승인 없이 확정하지 않았는가.
- 원본을 덮어쓰지 않았고 가능한 검증을 실행했는가.
