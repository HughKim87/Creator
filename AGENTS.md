# AGENTS.md

- 상태: active — 프로젝트 시작 라우터.
- 먼저 `PROJECT_RULES.md`를 끝까지 읽는다.
- 다음으로 `SESSION_HANDOFF.md`에서 현재 단계와 첫 다음 행동을 확인한다.
- 실행이 필요할 때만 `docs/REBUILD_PLAN.md`의 현재 단계 절을 읽는다.
- 기존 기능을 추가·교체·제거할 때는 먼저 `docs/INHERITANCE_MAP.md`의 해당 항목을 확인한다.
- `backup/`은 읽기 전용 보존 영역이다. 현재 단계가 지정한 파일만 선별해서 읽는다.
- 규칙의 정본은 `PROJECT_RULES.md` 하나뿐이다.
