# 단계 00 작업 지시서: 기준선 동결과 이식 목록 확정

## 1. 단계 목적

구체계의 정상 자산과 알려진 결함을 변경 없이 기록하여 이후 이식과 롤백의 기준선을
만든다. 이 단계는 구현 단계가 아니며 신규 패키지 구조나 기능을 만들지 않는다.

## 2. 사전조건

- 사용자가 단계 00의 읽기·진단 작업과 증거 파일 생성을 승인했다.
- `AGENT_EXECUTION_PLAN.md`를 읽었다.
- 현재 `backup/` 이동이 의도적인 구신 분리임을 확인했다.
- 기존 미추적 파일을 삭제·추적·이동하지 않는다.

## 3. 역할과 병렬화

### 기준선 인벤토리 에이전트

- 읽기: 전체 저장소와 `backup/`
- 쓰기: `reports/rebuild/stage-00/`만
- 역할: 파일 목록, 크기, 해시, Git 경계, 이식 후보 분류

### 동작 기준선 에이전트

- 읽기: `backup/tools/`, `backup/tests/`, 관련 계약
- 쓰기: `reports/rebuild/stage-00/`의 별도 증거 파일만
- 역할: 직접 실행과 래퍼 실행의 차이, 통과 테스트, 알려진 실패 기록

### 검증 에이전트

- 읽기 전용으로 앞선 두 결과를 교차검증한다.
- `backup/` 무변경과 증거 완전성을 확인한다.

인벤토리와 동작 기준선 수집은 병렬 가능하지만 같은 보고서 파일을 동시에 수정하지
않는다. 조정 에이전트가 마지막에 통합한다.

## 4. 허용 변경

- `reports/rebuild/stage-00/` 아래 신규 보고서와 증거
- 사용자가 별도 승인한 경우에만 보존용 Git 태그

## 5. 금지 변경

- `backup/` 내부 모든 변경
- 기존 루트 문서, 훅, CI, `.gitignore` 수정
- 구체계 코드를 현재 루트로 복사
- 미디어 샘플을 Git에 추가
- 테스트 실패를 고치기 위한 코드 수정
- 사용자 승인 없는 커밋·태그·푸시

## 6. 생성할 산출물

```text
reports/rebuild/stage-00/
├─ STAGE_REPORT.md
├─ REPOSITORY_BASELINE.md
├─ BACKUP_MANIFEST.json
├─ MIGRATION_CATALOG.md
├─ BASELINE_TESTS.md
├─ KNOWN_FAILURES.md
├─ GOLDEN_FIXTURE_PLAN.md
└─ evidence/
```

### `BACKUP_MANIFEST.json`

각 항목에 다음 필드를 둔다.

- 상대 경로
- 파일 종류
- 바이트 크기
- SHA-256
- Git 추적 여부
- 역할 추정: code, test, contract, doc, input, output, temporary, unknown
- 마지막 수정 시각은 참고값으로만 기록

대용량 파일은 메모리에 전부 올리지 말고 스트리밍 방식으로 해시한다. 해시 수집 시간이
지나치게 길면 코드·계약·테스트를 먼저 완료하고 미디어 해시 진행 상태를 보고한다.

비밀 가능 파일은 예외로 처리한다. `.env`, `*.pem`, 개인키, 인증서 개인키, 자격 증명
저장소는 내용을 열거나 해시하거나 로그에 출력하지 않는다. 경로 패턴과
`excluded_sensitive` 분류만 기록하며, 민감 여부가 불명확하면 중단하고 사용자 판단을
받는다. 이 예외는 누락이 아니라 의도적인 비수집으로 보고서에 설명한다.

### `MIGRATION_CATALOG.md`

파일 또는 기능 묶음을 다음 네 등급으로 분류한다.

- `PRESERVE`: 동작과 테스트를 그대로 보존
- `PORT_WITH_TESTS`: 특성 테스트를 만든 뒤 신규 구조에 이식
- `REDESIGN`: 개념은 유지하지만 구현은 교체
- `RETIRE`: 신규 구조에서 사용하지 않음

각 분류에는 근거, 연결된 테스트, 의존 파일, 이식 단계, 롤백 방법을 기록한다.

## 7. 세부 작업 순서

### 작업 00-1: 저장소 상태 캡처

다음을 읽기 전용으로 기록한다.

- 현재 브랜치와 HEAD
- 원격 주소
- Git 상태
- `core.hooksPath`
- 루트의 추적·미추적 파일
- `backup/` 파일 수와 크기
- 현재 CI와 훅이 참조하는 경로의 존재 여부

사실과 추론을 분리한다. 예를 들어 “스크립트가 없음”은 사실이고 “CI가 실패할 것”은
로컬 구조에 근거한 추론이다. 원격 Actions 결과를 확인하지 못했다면 그렇게 기록한다.

### 작업 00-2: 구체계 인벤토리와 해시

1. 코드, 테스트, 계약, 문서, 입력, 출력을 구분한다.
2. 코드·테스트·계약의 SHA-256을 우선 생성한다.
3. 민감 제외 정책에 걸리지 않는 입력·출력의 크기와 해시를 생성한다.
4. 임시 파일과 생성 가능 파일을 별도 분류한다.
5. 동일 입력에서 매니페스트를 두 번 생성하고, 참고용 수정 시각을 제외한 정규화 JSON
   또는 바이트 결과가 동일한지 확인한다.
6. 인벤토리 실행 전후 `backup/`의 Git diff, 전체 파일 목록, 크기와 핵심 해시가
   변하지 않았는지 확인한다. Git 미추적 파일도 비교 대상이다.

### 작업 00-3: 동작 기준선 수집

1. `backup/` 자체에서 테스트를 실행하지 않는다. 승인된 별도 legacy worktree 또는
   폐기 가능한 임시 checkout에서 `PYTHONDONTWRITEBYTECODE=1`과 저장소 밖 임시 산출물
   경로를 사용한다. worktree 생성·정리는 사용자 승인 범위와 경로를 먼저 기록한다.
2. 구체계 테스트를 래퍼가 아닌 실제 Python 실행 파일로 직접 실행한다.
3. 통과·실패·실행 불가를 구분한다.
4. 실패가 `backup/` 이동에 따른 루트 결합인지 핵심 로직 결함인지 분류한다.
5. 과거 래퍼로도 같은 명령을 실행하여 실제 종료 코드와 래퍼 종료 코드를 비교한다.
6. 자식 프로세스가 7로 종료될 때 최종 코드가 무엇인지 명시한다.
7. 테스트가 존재하지 않는 실패 경로를 `KNOWN_FAILURES.md`에 기록한다.

이 단계에서 발견한 결함을 수정하지 않는다. 단계 01 또는 03의 입력으로 넘긴다.

### 작업 00-4: 보존 가치 확정

최소한 다음 항목을 개별 평가한다.

- 소스 프레임 생성과 자산 등록
- `source_id` 및 원본 시간 좌표
- 동기화 검사
- Premiere XML 생성
- 편집 품질 감사
- 편집 세대·승인 기준선
- 단계 계약과 `default_deny`
- 사람 A/V 승인
- FFmpeg/remux 실행
- 실행 래퍼와 Git 루트 탐지
- 문서 검사와 상태 제어 도구

통과 테스트가 있는 기능은 해당 테스트 이름과 연결한다.

### 작업 00-5: 골든 픽스처 계획

두 종류를 구분한다.

1. 기술 픽스처: 테스트 시 생성 가능한 짧은 합성 영상·음성. Git에는 생성 스크립트와
   기대 메타데이터만 둔다.
2. 실제 파일럿: 외부 작업 공간에 보관되는 대표 영상. Git에는 경로가 아닌 논리 ID,
   해시, 길이, 코덱, 승인자만 기록한다.

저작권·개인정보가 있는 미디어를 보고서나 외부 서비스에 업로드하지 않는다.

### 작업 00-6: 롤백 지점 제안

- 구체계 마지막 정상 커밋을 식별한다.
- 제안 태그 이름과 대상 SHA를 보고한다.
- 사용자가 승인한 뒤에만 태그를 만든다.
- 향후 구체계 실행은 `backup/` 중첩 경로가 아니라 해당 태그의 별도 worktree에서
  수행하도록 기록한다.

## 8. 필수 검증

- 기준선 수집 전후 `backup/` Git diff 0건
- 기준선 수집 전후 Git 미추적 파일을 포함한 전체 파일 목록 변화 0건
- 기준선 수집 전후 핵심 파일 해시 동일
- 민감 제외 파일의 내용·해시가 보고서와 로그에 없음
- 매니페스트 2회 생성의 결정적 필드가 동일
- 모든 파일이 한 분류에 포함되거나 `unknown`으로 명시됨
- 통과 테스트 수와 실패 테스트 수가 실제 실행 로그와 일치
- 래퍼와 직접 실행 결과의 종료 코드가 각각 기록됨
- 골든 픽스처에 실제 미디어 Git 추가가 없음
- 원격 CI를 확인하지 못한 경우 확인한 것처럼 쓰지 않음

## 9. 완료 기준

- 구체계의 보존·이식·재설계·폐기 목록이 사용자 검토 가능한 수준으로 완성됨
- 알려진 거짓 성공 경로가 재현 절차와 함께 기록됨
- 이후 단계가 비교할 골든 기준과 롤백 SHA가 식별됨
- `backup/`과 기존 원본의 변경이 0건임
- 검증 에이전트가 증거 패키지를 승인함
- 사용자가 단계 01 착수를 별도로 승인함

## 10. 중단 조건

- 인벤토리 과정에서 파일이 변경됨
- 구체계 테스트가 원본이나 산출물을 덮어쓰려 함
- 마지막 정상 커밋을 식별할 수 없음
- 대용량 파일 해시 때문에 시스템 안정성이 저하됨
- 기존 미추적 사용자 파일과 산출물 경로가 충돌함

## 11. 롤백

이 단계는 읽기 중심이므로 롤백 대상은 신규 보고서뿐이다. 보고서 삭제 또는 태그
변경이 필요하면 사용자 승인을 다시 받는다. `backup/`을 원위치로 이동하는 것은 롤백
절차가 아니다.
