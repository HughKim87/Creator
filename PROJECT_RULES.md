# PROJECT_RULES.md — 김실버유튜브 전역 지침 (Single Source of Truth)

이 파일은 이 하위 프로젝트의 유일한 규칙 원본이다. 모든 AI 에이전트
(Claude, Codex, Gemini 등)는 이 파일을 읽고 따른다.
`CLAUDE.md`, `AGENTS.md`, `GEMINI.md`는 이 파일을 가리키는 얇은 포인터다 —
포인터 파일에 규칙을 추가하지 말고, 이 파일만 수정한다.

## 프로젝트 계층 (Tier 2)

이 프로젝트는 상위 프로젝트 **Building WorkFlow**의 Tier-2 하위 프로젝트다.

- 위치: `Building WorkFlow/Workspace/김실버유튜브/`
- 상위 원본: `Building WorkFlow/PROJECT_RULES.md` (Tier 1) — 항상 먼저 읽는다.
- 충돌 시 우선순위:
  - 보안/안전·백업 규칙: Tier 1이 항상 우선. 여기서는 강화만 가능, 약화 불가.
  - 도메인 작업 방식(유튜브 제작): 이 파일과 하위 문서가 우선.
- 이 프로젝트 작업 중에는 Tier-1 프레임워크 파일과 다른 하위 프로젝트의
  파일을 추가·수정·삭제하지 않는다.

## 프로젝트 목적

유튜브 채널 "신입 아재 유튜버, 김실버😎"의 채널 관리,
업로드할 영상에 대한 전반적인 기획·제안, 영상 편집, 자막 생성,
하이라이트 장면 추천 등 제작 전 과정 지원.

## 문서 적용 순서

```text
상위 PROJECT_RULES.md (Building WorkFlow, Tier 1)
↓
이 파일 (김실버유튜브 전역 지침, Tier 2)
↓
00_공통_작업원칙.md   — 원본 보호, 파일 생성/삭제, 의도 확인, 중복 방지
↓
01_유튜브_제작_워크플로우.md — 전체 작업 순서와 단계별 스킬 호출 기준
↓
skills/*/SKILL.md    — 각 작업을 실제로 수행하는 독립 스킬
```

## 중복 방지

- 워크플로우는 스킬의 세부 절차를 반복하지 않고, 스킬은 전체 워크플로우를
  반복하지 않는다.
- 여러 문서에 반복되는 규칙은 상위 문서 하나로 분리한다.

## 이 프로젝트의 작업 환경 적용

상위 Tier-1의 `Operational Discipline`을 따른다. 이 프로젝트에서 추가로 고정할
실행 도구는 프로젝트 내부 FFmpeg다.

- 영상 분석, 프레임 추출, 러프컷 생성에는 `tools/ffmpeg/bin/`의 FFmpeg를 우선한다.
- 사용자 PC에서 실행할 스크립트는 `.ps1` 대신 `.bat`로 제공한다.
  (사용자 PowerShell 실행 정책이 `.ps1`을 차단함 — 2026-07-02 확인)

## 영상 편집 자동화 검증 적용

상위 Tier-1의 `Verification Levels`를 따른다. 이 프로젝트에서 `App-validated`는
Premiere Pro에서 가져오기, 미디어 연결, 오디오, 시퀀스 재생까지 확인된 상태를 뜻한다.

- Premiere에서 직접 열어보지 않았으면 "Premiere 사용 가능 확정"이라고 쓰지 않는다.

## 컷리스트 기준 관리

상위 Tier-1의 `Source-of-Truth Inputs`를 따른다. 이 프로젝트에서 영상 편집 산출물의
기준 입력은 최신 컷리스트다.

- 화면 검증, 라운드맵, 자막 분석으로 더 나은 구간이 확인되면 생성 작업 전에 기준 컷리스트를 먼저 갱신한다.
- XML, EDL, 러프컷 mp4는 항상 최신 기준 컷리스트에서 다시 생성한다.
- `한 판 시작-결과-반응`처럼 보존 규칙이 있는 구간은 컷리스트에 그 판단 근거를 남긴다.

## 영상 길이 원칙

- 편집 영상의 길이는 고정 목표를 두지 않는다. 원본마다 달라진다.
- 길이는 "몇 분을 맞출까"가 아니라 "각 컷이 재미·퀄리티에 기여하는가"로
  keep/제외를 판단한 결과로 자연히 정해진다.
- 분석·컷리스트 문서에 길이 목표를 기준으로 적지 않는다. 합계 길이는
  참고 측정치로만 기록한다. (2026-07-05 사용자 확정)

## 파일 백업

- 보존 가치가 있는 파일을 수정하기 전에 `temp/backups/YYYY-MM-DD/`로 백업한다.
- 삭제 대신 백업 폴더로 이동을 우선한다. (상위 Tier-1 규칙과 동일)
