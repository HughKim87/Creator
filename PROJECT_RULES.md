# PROJECT_RULES.md — 김실버유튜브 전역 지침 (Single Source of Truth)

이 파일은 이 하위 프로젝트의 유일한 규칙 원본이다. 모든 AI 에이전트
(Claude, Codex, Gemini 등)는 이 파일을 읽고 따른다.
`CLAUDE.md`, `AGENTS.md`, `GEMINI.md`는 이 파일을 가리키는 얇은 포인터다 —
포인터 파일에 규칙을 추가하지 말고, 이 파일만 수정한다.

## 프로젝트 계층 (Tier 2)

이 프로젝트는 상위 프로젝트 **Building WorkFlow**의 Tier-2 하위 프로젝트다.

- 위치: `Building WorkFlow/Workspace/김실버유튜브/`
- 상위 원본: `Building WorkFlow/PROJECT_RULES.md` (Tier 1) — 항상 먼저 읽는다.
- 충돌 시 우선순위:
  - 보안/안전·백업 규칙: Tier 1이 항상 우선. 여기서는 강화만 가능, 약화 불가.
  - 도메인 작업 방식(유튜브 제작): 이 파일과 하위 문서가 우선.
- 이 프로젝트 작업 중에는 Tier-1 프레임워크 파일과 다른 하위 프로젝트의
  파일을 추가·수정·삭제하지 않는다.

## 프로젝트 목적

유튜브 채널 "신입 아재 유튜버, 김실버😎"의 채널 관리,
업로드할 영상에 대한 전반적인 기획·제안, 영상 편집, 자막 생성,
하이라이트 장면 추천 등 제작 전 과정 지원.

## 작업 방식 — 단계별로 확실히 (사용자 확정 2026-07-07)

- **한 번에 한 단계씩 진행한다.** 여러 개선을 한꺼번에 묶어 지시·수행하지 않는다.
  각 단계의 규격·기준을 확실히 확정한 뒤에만 다음 단계로 넘어간다.
- 하위 단계(편집 등)에서 원하는 퀄리티가 반복해서 안 나오면, 하위를 더 손대기 전에
  **상위 단계의 기준부터 다시 확인한다.** 문제는 대개 상위 단계에 있다.

## 문서 적용 순서

```text
상위 PROJECT_RULES.md (Building WorkFlow, Tier 1)
↓
이 파일 (김실버유튜브 전역 지침, Tier 2)
↓
00_공통_작업원칙.md   — 원본 보호, 파일 생성/삭제, 의도 확인, 중복 방지
↓
01_유튜브_제작_워크플로우.md — 전체 작업 순서와 단계별 스킬 호출 기준
↓
skills/*/SKILL.md    — 각 작업을 실제로 수행하는 독립 스킬
```

## 중복 방지

- 워크플로우는 스킬의 세부 절차를 반복하지 않고, 스킬은 전체 워크플로우를
  반복하지 않는다.
- 여러 문서에 반복되는 규칙은 상위 문서 하나로 분리한다.

## 이 프로젝트의 작업 환경 적용

상위 Tier-1의 `Operational Discipline`을 따른다. 이 프로젝트에서 추가로 고정할
실행 도구는 프로젝트 내부 FFmpeg다.

- 영상 분석, 프레임 추출, 러프컷 생성에는 `tools/ffmpeg/bin/`의 FFmpeg를 우선한다.
- 사용자 PC에서 실행할 스크립트는 `.ps1` 대신 `.bat`로 제공한다.
  (사용자 PowerShell 실행 정책이 `.ps1`을 차단함 — 2026-07-02 확인)
- **세션에서 만든 검증·분석 스크립트는 `tools/`에 영속화한다.** 샌드박스
  스크래치에 두면 세션 종료로 소멸해 재구축 낭비가 반복된다(2026-07-06 확인).
  현재: `tools/synccheck/` (경계 스캔 — 세션 마운트 경로 상수만 교체 후 사용,
  의존성 `pip install webrtcvad-wheels --break-system-packages`).
- Premiere XML 생성은 `--no-candidates` 고정(사용자 확정 — 후보클립 빈 금지,
  원본+시퀀스만).

## 영상 편집 자동화 검증 적용

상위 Tier-1의 `Verification Levels`를 따른다. 이 프로젝트에서 `App-validated`는
Premiere Pro에서 가져오기, 미디어 연결, 오디오, 시퀀스 재생까지 확인된 상태를 뜻한다.

- Premiere에서 직접 열어보지 않았으면 "Premiere 사용 가능 확정"이라고 쓰지 않는다.

## 기획 원칙 — 대사에서 메시지 도출 (사용자 확정 2026-07-07)

편집은 기획의 하위 단계다. 기획(이 영상이 하려는 말)이 먼저 확정돼야 편집이 의미를 갖는다.

- **완성 영상이 담을 메시지·내용은 원본 대사에서 도출·추적 가능해야 한다.** 지어내지 않는다.
  메시지의 모든 조각을 실제 대사 인용으로 되짚을 수 없으면 그 메시지는 폐기한다.
- 기획은 "반응 큰 순간 모으기"가 아니라 "대사에서 이 영상이 하려는 말 찾기"다.
  하이라이트·컷은 이야기를 만드는 게 아니라 섬긴다(아래 편집 공통 원칙 1과 연결).
- 절차·완료 게이트는 `기획_리서치/기획단계_규격_2026-07-06.md`를 따른다
  (통독 → 컨트롤링 아이디어 → 스파인 / 게이트 G1 근거·G2 오디오 성립·G3 기여).

## 편집 공통 원칙 (전 영상 적용, 2026-07-05~06 확립)

1. **기획(이야기) 먼저, 컷은 그 다음.** 컷 취사·배열 판단의 상위 기준은 항상
   해당 영상의 확정 구성안이다. 구성안 없이 컷 목록부터 다듬지 않는다.
2. **문장 단위 완결 필수.** 말이 시작됐으면 끝맺음까지 포함한다. 말 중간 컷 금지.
   이어지는 문장은 연관도로 포함 여부를 판단한다.
3. **훅은 액션 중간에서 끊지 않고, 10초 이상으로 만든다.**
4. **토크·메뉴 구간은 미세 점프컷으로 밀도를 최대화한다.** (2026-07-06 사용자
   Premiere 직접 시연으로 개정 — 종전 "미세 점프컷 금지" 규칙 폐기)
   - 토크·메뉴 조작 중 **0.5초 이상의 쉼·호흡은 제거**한다. 0.3초 단위 미세
     점프컷 허용.
   - 무음만이 아니라 **내용 기준으로도 삭제**한다: 말실수·정정 재시작("아니지"류),
     기능성 전환 멘트("한번 해볼게요"·"일단 플레이 해보겠습니다"류), 방송 관리
     멘트(사운드 체크·시청자 확인)는 문장이 완결돼 있어도 삭제.
   - 문장 keep/drop 기준은 **정보·분위기 기여 여부**다. 준비 잡담은 버리고,
     분위기 설정 문장은 살린다.
   - 단, **게임플레이 액션이 화면에서 진행 중인 구간은 소스 연속을 유지**한다.
     미세 점프컷은 토크·메뉴·정지 화면에만 적용한다.
   - 컷 꼬리는 발화 종료 직후 즉시 끊는다(여운 패딩 없음). 문장 완결 원칙(2번)은
     유지하되, 완결 후 무음을 남기지 않는다.
5. **전환 효과는 절제.** 게임 실황은 하드컷 기본. 디졸브·페이드는 시간 경과와
   막(챕터) 전환에만. 컷 연결 세부 기법(J/L컷·액션 중 컷·자막 브릿지)은
   `기획_리서치/컷_연결_자연스러움_리서치_2026-07-06.md`를 따른다.

## 컷 경계 검증 (전 영상 적용, 2026-07-06 확정)

v8 사고에서 확립: Whisper 자막(SRT) 블록 가장자리는 실제 발화와 0.3~1.5초
어긋난다. 이를 컷 경계로 그대로 쓰면 단어 머리 잘림·다음 문장 유입이 누적된다.

- **SRT 블록 가장자리를 컷 경계로 쓰지 않는다.** 경계는 VAD 실측(발화 온셋
  −0.4s / 종료는 발화 끝 직후, +0.4s 일괄 패딩 금지 — 2026-07-06 시연 개정)에
  자막 블록 가드(이웃 문장 침범 금지)를 걸어 확정한다.
- **XML 생성 전 경계 스캔이 필수 게이트다.** 도구: `tools/synccheck/full_scan.py`.
- **VAD 단독 판단은 방향에 따라 다르다.** 확장(여유 추가)은 VAD 단독 허용,
  **트림(내용 제거)은 자막 원문 교차확인 필수.** VAD는 조용한 발화를 놓치고
  웃음·호흡을 발화로 오인한다(2026-07-06 실측 2건).
- 이웃 문장에 막힌 0.3초 이하 잔차는 Premiere 파형 스냅으로 마무리한다.

## 컷리스트 기준 관리

상위 Tier-1의 `Source-of-Truth Inputs`를 따른다. 이 프로젝트에서 영상 편집 산출물의
기준 입력은 최신 컷리스트다.

- 화면 검증, 라운드맵, 자막 분석으로 더 나은 구간이 확인되면 생성 작업 전에 기준 컷리스트를 먼저 갱신한다.
- XML, EDL, 러프컷 mp4는 항상 최신 기준 컷리스트에서 다시 생성한다.
- `한 판 시작-결과-반응`처럼 보존 규칙이 있는 구간은 컷리스트에 그 판단 근거를 남긴다.

## 영상 길이 원칙

- 편집 영상의 길이는 고정 목표를 두지 않는다. 원본마다 달라진다.
- 길이는 "몇 분을 맞출까"가 아니라 "각 컷이 재미·퀄리티에 기여하는가"로
  keep/제외를 판단한 결과로 자연히 정해진다.
- 분석·컷리스트 문서에 길이 목표를 기준으로 적지 않는다. 합계 길이는
  참고 측정치로만 기록한다. (2026-07-05 사용자 확정)

## 파일 백업

- 보존 가치가 있는 파일을 수정하기 전에 `temp/backups/YYYY-MM-DD/`로 백업한다.
- 삭제 대신 백업 폴더로 이동을 우선한다. (상위 Tier-1 규칙과 동일)
