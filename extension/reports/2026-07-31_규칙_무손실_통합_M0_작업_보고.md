# 규칙 무손실 통합 개선 M0 최종 작업 보고

- 문서 유형: `final-work-report` / `reference-evidence`
- 범위: M0 규칙 계보·손실 감사의 S1~S4와 exit/transition closeout
- 현재 상태: M0 passed. M1~M4는 시작하지 않음.
- 기준 checkpoint: `48a11dd` (`docs: M0 규칙 계보·손실 감사 완료`)
- 현재 reviewer: 이 세션의 Codex agent. Claude 보고서 작성자와 분리한다.
- 보호 경계: `inputs/`, `outputs/`, secrets, 외부 상태, dependency 설치, 삭제·이동, `core/**` mutation 모두 0.

## 결론

M0의 목표인 현재 규칙 의미와 직접 역사 계보의 대조, 손실 후보 분류, M1 입력 고정을 완료했다. 현재 의미 78개는 모두 source anchor와 current owner에 연결됐고, 19개 활성 rule-surface 경로의 계보를 `git log --follow`로 확인했다. 경로 전환·삭제 predecessor 4건은 별도 보완해 successor 또는 공백을 기록했다.

M0는 성공 게이트를 통과했지만, 초기 S1 corpus 수치가 exact glob과 맞지 않아 post-check에서 정정했다. 정정 후 회귀·maintenance·문서 예산을 다시 검증했으며, 이 수치 보정은 최종 보고 checkpoint에 함께 보존한다.

## 목표-결과 교차검증과 점수

점수는 5점 만점이다. `passed`는 게이트 통과, 점수는 결과의 정확성·재현성·범위 준수까지 반영한다.

| 단계/슬라이스 | 목표 | 실제 결과 | 점수 | 이유 |
|---|---|---|---:|---|
| M0-S1 | corpus·현재 의미 인벤토리·D3/D4 고정 | 19/9,759, 38/19,653, candidate 1/675로 재측정; 의미 78개·미매핑 0 | 4/5 | 초기 19/9,650·37/18,433 수치를 후속 직접 측정으로 정정한 재작업이 있었음 |
| M0-S2 | D6 순서의 직접 계보 추적 | 19개 path·78/78 anchor, predecessor 4건, `backup/**` bulk read 0 | 5/5 | follow와 필요한 삭제·rename 보완을 모두 실행하고 successor/gap을 기록함 |
| M0-S3 | 보존·승계·폐기·보류 판정 | L01~L12 전부 evidence·owner·disposition 기록 | 5/5 | candidate-loss·migrated·superseded·preserved·task-specific을 분리함 |
| M0-S4 | M1 입력과 최소 변경 후보 고정 | Core 후보 5경로, extension-only 대안, 예상 효과·검증 gate를 고정 | 4/5 | 입력은 완성했으나 Core mutation과 효과 입증은 승인·후속 단계 전까지 보류함 |
| M0 전체 | 손실 없는 계보 감사 후 M1 진입 조건 확정 | exit/transition gate와 phase commit 완료 | 4/5 | 범위·보호·Core 경계는 지켰고 corpus 정정 비용을 반영함 |
| M1~M4 | 후속 보존검증·통합·종료·문서 처분 | 미착수 | N/A | M1은 exact Core 승인, M4는 exact 삭제 목록 사용자 승인이 entry 조건임 |

## 판정 결과

### 현재 78개 의미

- `preserved`: 73개
- `migrated`: 5개(파일 정리·추출 owner 이동을 포함)
- source anchor·evidence·disposition은 [M0 규칙 보존 지도](../work/rule-preservation/M0_RULE_CONSERVATION_MAP.md)가 행 단위로 소유한다.

### 과거 후보 L01~L12

- `candidate-loss`: L01, L02, L03, L05 및 `rules/history-review.md`의 exact historical-read delta
- `migrated`: L04, L08, L09, L10
- `superseded`: L06, L11 — 현재 자율성 정책과 충돌하며 사용자 의도 미확정
- `preserved`: L07 — 도구 성공과 콘텐츠 승인 분리는 현재 문서·영상 validation owner에 존재
- `task-specific`: L12 — 보고서의 failure 통합 표본 주장이며 M1 변경 대상 아님

이 분류는 Claude 보고서의 과거 주장을 현재 권위로 승격한 것이 아니다. 과거 보고서는 `report-only` 또는 source anchor로만 사용했고, 현재 파일·Git·테스트는 이 세션에서 직접 확인했다.

## 근거 수치와 검증 결과

| 항목 | 직접 측정 결과 | evidence label |
|---|---:|---|
| `rule-surface` | 19 files / 9,759 tokens / 59,493 chars | direct-remeasurement |
| `normative-corpus` | 38 files / 19,653 tokens / 130,428 chars | direct-remeasurement |
| candidate/reference | 1 file / 675 tokens / 2,772 chars | direct-remeasurement |
| 보존 의미 | 78/78 anchor·owner 연결, 미매핑 0 | git-lineage + repository-source |
| M0 보존 지도 | 322 lines / 29,756 chars / NUL 0 / trailing whitespace 0 | direct-remeasurement |
| Core 회귀 | 139 tests passed | direct-remeasurement |
| Extension 회귀 | 132 tests passed | direct-remeasurement |
| maintenance-verify | errors 0 / drift 0 / duplicates 0; documents 87 / links 122 / Python 31 / schemas 14 | direct-remeasurement |

이전 `37 files·18,844 words`, `19,519 words`, `37 files·18,433 tokens`는 exact glob 또는 재현 명령이 현재 결과와 맞지 않아 후속 성공 기준에서 제외했다. 이는 개선 효과 수치가 아니라 측정 정정 결과다.

## 효과 입증 여부

현재 입증 가능한 효과는 “규칙 의미의 계보·owner·증거를 한 지도에서 복원할 수 있음”과 “변경 후 구조·회귀 검증이 통과함”까지다. 영상 작업 시간, 수동 입력, 재작업, 규칙 손실 재발률의 개선은 비교용 pilot 데이터가 없어 입증하지 않았다.

M1/M2가 승인·완료되면 검증할 예상 효과는 candidate-loss 0, 중복 owner 0, 신규 active rule 0이다. 현재는 `미검증 예상`이며 성공으로 계산하지 않는다.

## 권장 다음 단계와 승인 경계

권장안은 새 규칙을 만들지 않고 기존 owner를 강화하는 M1 검증부터 시작하는 것이다. M1에서 검토할 최소 Core 후보는 다음 5개다.

- `PROJECT_RULES.md`
- `core/rules/boundary-routing-and-dependency.md`
- `core/rules/cross-validation.md`
- `core/rules/document-work.md`
- `core/rules/file-extraction.md`

L09는 영상 validation extension-only 대안이 가능하다. L01~L05·L08·L10은 foundation 범용 경계이므로 extension-only로 대체하지 못하는 이유를 M1에서 검증한다. L06·L11은 사용자 의도 확인 전 복원하지 않는다. M4 삭제·이동은 M3 통과 후 exact 목록을 별도 승인받을 때까지 실행하지 않는다.

## 변경·커밋·잔여 위험

- M0 단계 커밋: `48a11dd`
- M0 closeout에서 변경한 정본: `SESSION_HANDOFF.md`, M0 phase-design, M0 conservation map 및 이 보고서
- `M0-T2` rule audit: matched rule 모두 무결점 적용; owner 보강·failure 승격·새 owner 생성 0
- 잔여 blocker: 위 5개 Core 경로에 대한 현재 대화의 exact 승인 부재, L06/L11 사용자 의도 미확정
- 복구: 모든 변경은 Git으로 복구 가능하며 보호 경로는 stage·commit하지 않았다.
