# e4161fd 장점 선별 개선 적용 최종 보고

- 작성일: 2026-07-27
- 비교 기준: `e4161fd1a82649e1ef4bea164a51996d6b2679a5`
- 적용 시작 기준: `96c2c79`
- 결과 상태: 계획 G0~G5 완료, 로컬 커밋 완료, push 0
- 실제 앱 검증 상태: 구조 검증 완료, Premiere CS6 import·재생은 미실행

## 1. 결론

이전 버전의 장점을 파일 묶음이나 작업 원문으로 복원하지 않고 현재 프로젝트의 작은 실행 계약과 회귀 가능한 기능으로 선별 적용했다.

채택한 장점은 네 가지다.

1. Claude가 공통 시작 규칙을 놓치지 않는 얇은 진입점
2. 원본을 바꾸지 않는 SRT 검증·명시 정리
3. legacy 9열 cut CSV의 무추론 이관과 다중 오류 진단
4. 기존 XML을 깨지 않는 명시적 Premiere CS6 FCP XML v4 profile

기존 운영 규칙 R01~R12와 재현 사례 TC01~TC12는 늘리지 않았다. 새 기능은 운영 계약의 실행 경계와 코드·테스트에만 반영했다.

## 2. 적용 범위와 제외 범위

적용 원칙:

- 이전 commit은 비교 근거로만 사용하고 merge·cherry-pick하지 않음
- 이전 archive·세션 원문·버전별 보고·작업별 고정값을 활성 구조로 복원하지 않음
- 현재 schema와 `video_editing` package에 맞춰 일반화
- 실제 보호 데이터, `core/`, 외부 시스템을 건드리지 않음
- 각 기능은 합성 fixture와 성공 gate를 통과한 뒤 커밋

의도적으로 제외한 항목:

- 저장소 내부 백업·archive 복제
- 과거 작업의 frame·경로·UUID·60fps·1080p·48kHz·stereo 고정값
- 과거 문서 원문과 중복 guide
- 실제 Premiere import를 하지 않은 상태에서의 호환 완료 주장

## 3. 단계별 결과와 커밋

| 단계 | 결과 | 성공 gate | 커밋 |
|---|---|---|---|
| G0 | 선별 적용 계획 고정 | 기준 HEAD·변경 1개·보호/core 변경 0 | `b6e8341` |
| G1 | `CLAUDE.md`를 `@AGENTS.md` 단일 참조로 추가 | owner·보호 경로·skill route 회귀 3개 | `9497a52` |
| G2 | strict UTF-8 SRT 검증·명시 보정·제외·원자적 출력 | 신규 9개, 당시 Extension 87개 통과 | `8520f48` |
| G3 | timeline 다중 issue와 legacy CSV importer | 신규 11개, 당시 Extension 98개 통과 | `a8088cd` |
| G4 | 기본 `sequence-v5`와 명시적 `premiere-cs6-v4` | 신규 8개, 당시 Extension 106개 통과 | `4359981` |
| G5-1 | SRT→CSV import→의미 gate→CS6 XML 수직 acceptance | 합성 원본 hash 불변·산출물 exact 일치 | `e2abd0e` |
| G5-2 | 운영 계약·README·handoff·최종 보고 동기화 | Core 110·Extension 107 통합 회귀 | 이 문서를 포함한 최종 문서 커밋 |

## 4. 기능별 검증 결과

### Claude 시작 경로

- `@AGENTS.md` 참조 1개
- policy owner는 `PROJECT_RULES.md`, 현재 상태 owner는 `SESSION_HANDOFF.md`
- 과거의 잘못된 handoff 크기 전제 제거
- 보호 경로와 `.agents/skills/` route만 Claude 도구 차이로 유지

### SRT

- UTF-8·BOM·CRLF 처리
- index·timestamp·양수 길이·순서·겹침·미디어 종료 검증
- cue별 `start_ms`·`end_ms`와 명시 제외만 허용
- 본문과 원본 SHA-256 불변
- destination 부모 자동 생성 금지, 기본 비덮어쓰기
- 실패 시 destination·temporary file 0

### legacy CSV와 다중 진단

- 필수 열은 `idx,track,name,src_in,src_out,frames,tl_start,tl_end,gap_before`
- frame·timeline·gap·행 순서 보존
- 누락 열·정수·중복 idx·track·범위·연속성·A/V 오류를 결정적 순서로 보고
- issue 필드는 `code / message / track / clip_id / index`
- importer는 `edit_role: unclassified`, `semantic_gate: pending`만 생성
- 의미 검수 전 XML 생성 차단

### Premiere XML profile

- 기본 `sequence-v5`는 11,629 byte, SHA-256 `9553f10ace7728334187edaafe159d2a4a02b8afdcb21ad95ad65d0a226de100` 유지
- `premiere-cs6-v4`는 declaration·DOCTYPE·xmeml v4·project·bin·masterclip·sequence 생성
- source file 정의 1개와 동일 `file-1` reference 사용
- video/audio 범위·audio gap·channel별 sourcetrack·output channel·link 보존
- UUID는 canonical timeline에서 결정적으로 생성
- frame rate·해상도·sample rate·channel은 source metadata 사용
- 반환 상태는 `structure-validated`

## 5. 최종 성공 gate

실행 결과:

```text
Core:      110 / 110
Extension: 107 / 107
Total:     217 / 217
```

추가 확인:

- `git diff --check`: 통과
- UTF-8 strict decode·NUL 0: 통과
- `core/` 변경: 0
- 보호 데이터 접근·변경·commit: 0
- 요청하지 않은 미디어 산출물: 0
- push: 0

수직 acceptance는 다음 순서를 실제 API로 재현했다.

1. 합성 SRT 명시 보정·제외와 검증
2. 합성 video/audio CSV를 pending timeline JSON으로 이관
3. pending 상태에서 XML 생성 차단
4. 테스트가 의미 검수 주체를 명시한 뒤 timeline 재검증
5. CS6 v4 XML 생성과 구조 확인
6. SRT·CSV 세 원본 hash와 exact 파일 목록 대조

## 6. 한계와 다음 사용자 gate

`premiere-cs6-v4`는 XML 구조와 내부 참조를 검증했지만 실제 Premiere CS6에서 import·재생하지 않았다. 따라서 현재 주장은 구조 호환까지다.

다음 단계는 사용자가 exact 보호 원본·자막·출력과 목적을 지정한 별도 작업에서 수행한다.

1. 실제 source metadata로 timeline 생성
2. 의미 gate를 실제 편집 내용으로 통과
3. CS6 profile XML 생성
4. Premiere import
5. 오프라인 미디어·프레임 경계·audio channel·gap·재생 상태 검수
6. 실제 결과가 다를 때 최소 fixture와 규칙 조건만 보강

## 7. 작업 복기

잘된 점:

- 이전 버전 전체를 좋은 것으로 간주하지 않고 검증 가능한 네 능력만 분리했다.
- 과거 데이터를 참조하는 runtime 연결이 없어 이전 branch나 보고서가 없어도 기능이 동작한다.
- 의미 추론을 importer에 넣지 않고 pending gate로 넘겨 자동 승인을 막았다.
- 현재 XML byte 회귀를 고정한 뒤 새 profile을 병렬 추가해 회귀 위험을 제한했다.
- 문서 규칙 수를 늘리지 않고 코드·테스트가 세부 동작을 소유하게 했다.

개선할 점:

- 첫 Extension 전체 회귀 실행에서 표준 `PYTHONPATH`의 `core/tests`가 빠져 `test_support` import가 한 번 실패했다. 기능 실패는 아니었고 README의 표준 명령으로 수정해 전체 통과를 확인했다.
- 구현과 회귀 코드는 늘어났다. 다음 개선은 독립 영상에서 반복되는 실패가 확인될 때만 추가하고, 단순 편의 기능으로 새 rule·skill을 만들지 않아야 한다.
- 실제 Premiere 검증이 없으므로 v4 profile을 완성 호환으로 승격하면 안 된다.

## 8. 최초 개선 목적에 대한 자체 평가

사용자가 처음 제시한 개선 목적은 다음과 같이 해석했다.

1. 과거 경험의 원문을 계속 복제하지 않는다.
2. 과거 실패에서 앞으로 재사용할 핵심 규칙을 추출한다.
3. 원문과 백업이 없어도 같은 실패를 예방할 수 있어야 한다.
4. 프로젝트 근간은 거대한 문서가 아니라 작고 최적화된 규칙·검증·성공 gate여야 한다.
5. 실제 편집 작업의 수동 반복과 실패 가능성을 줄여야 한다.

이 목적에 대한 자체 평가는 `대체로 충족했지만 완전 충족은 아니다`이다.

| 평가 항목 | 근거 | 자체 판정 |
|---|---|---|
| 과거 원문 비의존 | 이전 branch·archive·보고서 경로를 runtime이나 운영 계약이 참조하지 않음 | 충족 |
| 경험의 규칙·동작 압축 | SRT 원본 불변, pending 의미 gate, 다중 진단, XML profile을 코드와 fixture로 변환 | 충족 |
| 백업 없이 독립 동작 | 합성 입력만으로 수직 acceptance 통과 | 충족 |
| 운영 근간의 비대화 방지 | R01~R12와 TC01~TC12를 늘리지 않고 startup owner도 얇게 유지 | 충족 |
| 문서량 최적화 | 547줄 계획 보고서는 실행 통제에는 유용했지만 최초 목적에 비해 길었음 | 부분 충족 |
| 실제 실패 개선 효과 | 합성 회귀는 통과했지만 실제 Premiere import·실제 영상 편집 효과는 미검증 | 부분 충족 |

긍정적 판단:

- 과거 경험을 문장 단위로 보존하지 않고 실패를 차단하는 조건과 실행 가능한 회귀로 바꿨다.
- 운영 규칙 수를 늘리지 않아 다음 세션이 과거 분석 보고서를 읽을 필요가 없다.
- 구현·테스트의 증가는 원문 복제가 아니라 입력·출력·실패 조건을 실행 가능한 형태로 고정한 것이다.
- 완료 상세는 startup 문서가 아니라 Git과 사용자 요청 보고서에 남겼다.

미흡한 판단:

- 계획 보고서가 547줄이 된 것은 “근간이 거대한 원문이 되면 안 된다”는 목적에 비추어 과도했다. 내용은 원문 복사가 아니지만 다음 계획은 목표·변경·검증·gate·commit만 남겨 더 짧게 작성해야 한다.
- 최종 보고서 역시 감사 목적상 상세하지만 운영 owner가 아니다. 후속 세션이 이 보고서를 필수로 읽거나 내용을 다시 복제하면 최초 목적을 훼손한다.
- CS6 profile과 legacy importer의 실효성은 실제 독립 영상에서 아직 증명되지 않았다. 합성 테스트 통과만으로 핵심 근간으로 무기한 확장하면 안 된다.

따라서 이번 작업은 `원문 축적 방식에서 실행 가능한 규칙·gate 방식으로 전환`하는 데는 성공했지만, `문서량 최소화`와 `실제 현장 효과 입증`은 후속 검증이 남았다.

## 9. 프로젝트 목적과 구조 적합성 평가

프로젝트의 목적은 사용자의 수동 생산 작업을 줄이고, 승인된 목표와 안전 경계 안에서 에이전트가 자율적으로 YouTube 작업을 수행하도록 돕는 것이다. 구조적으로는 `core/`가 불변 기반을, `extension/`이 영상·YouTube workflow와 구현을 소유한다.

### 목적 적합성

| 개선 | 수동 작업 절감 | 안전한 자율 수행 | 최종 평가 |
|---|---|---|---|
| Claude 시작 경로 | 시작 규칙 누락과 재설명 감소 | 공통 정책 owner와 보호 경계를 그대로 사용 | 적합 |
| SRT 검증·정리 | cue 오류 확인과 명시 보정 반복 감소 | 원본 불변·비덮어쓰기·명시 제외만 허용 | 매우 적합 |
| 다중 오류 진단 | 한 번에 여러 오류를 확인해 수정 왕복 감소 | 결정적 issue와 실패 차단 | 매우 적합 |
| legacy CSV importer | 기존 cut 자료의 수동 재입력 감소 | 의미 추론 금지와 pending gate | 조건부 적합 |
| CS6 XML profile | XML 수작업 조립 감소 | 구조 검증과 profile 명시 선택 | 조건부 적합 |
| 수직 acceptance | 기능 간 수동 확인 감소 | pending 차단·hash·산출물 예산 검증 | 적합 |

`legacy CSV importer`와 `premiere-cs6-v4`를 조건부 적합으로 평가한 이유는 한 번의 과거 작업에서 필요성이 확인됐을 뿐 두 번째 독립 영상에서 반복 가치가 아직 증명되지 않았기 때문이다. 현재 구현은 일반화·격리되어 구조상 문제는 없지만, 실제 사용 없이 기능을 더 확장하는 것은 프로젝트의 최소 근간 원칙과 맞지 않는다.

### 구조 적합성

| 구조 원칙 | 적용 결과 | 판정 |
|---|---|---|
| `core/` 불변 | core 변경 0 | 충족 |
| 영상 기능은 `extension/` 소유 | 구현·schema·tests·운영 계약을 모두 extension에 배치 | 충족 |
| 단일 운영 owner | 영상 편집 행동 규칙은 기존 운영 계약 한 곳에만 반영 | 충족 |
| 최소 startup state | `CLAUDE.md`는 `@AGENTS.md`를 한 번 참조하는 14줄 adapter이고 handoff는 현재 상태만 유지 | 충족 |
| 보호 데이터 경계 | 실제 보호 데이터 접근·commit 0 | 충족 |
| 상세 이력은 Git 사용 | 단계별 로컬 commit 7개, 저장소 내부 backup 복제 0 | 충족 |
| 새 rule·skill 승격 절제 | R01~R12 유지, 새 skill 0 | 충족 |
| 실제 결과와 구조 검증 분리 | CS6 상태를 `structure-validated`로 제한 | 충족 |

구조적 배치는 적합하다. 특히 영상 기능을 이유로 `core/`를 확장하지 않았고, 보고서를 runtime owner나 startup 필수 문서로 만들지 않은 점이 프로젝트 정보 구조와 맞는다.

다만 계획 보고서와 최종 보고서가 모두 남아 있어 문서 수 자체는 증가했다. 두 문서는 사용자가 각각 명시적으로 요청한 감사 산출물이므로 허용되지만, 운영 계약에 내용을 재복제하거나 다음 세션의 필수 읽기 대상으로 승격해서는 안 된다.

## 10. 최종 자체 판정과 후속 기준

최종 판정:

- 최초 개선 목적 적합성: `대체로 적합`
- 프로젝트 목적 적합성: `적합`
- 프로젝트 구조 적합성: `적합`
- 실제 제작 효과 검증: `미완료`
- 문서량 최적화: `부분 충족`

이번 적용은 과거 원문을 다시 쌓는 방식보다 재사용 가능한 동작·차단 gate·회귀 fixture로 압축하는 방향에 가깝다. 따라서 방향은 맞았지만 실제 영상과 Premiere에서 효과가 확인되기 전까지 완성된 근간으로 과대평가하면 안 된다.

후속 작업은 다음 기준을 따른다.

1. 다음 실제 영상에서 수동 단계 감소, 오류 탐지, Premiere import·재생 결과를 확인한다.
2. 실패가 나오면 새 장문 보고서보다 최소 재현 fixture와 기존 규칙의 조건·예외를 먼저 보강한다.
3. legacy importer와 CS6 profile은 두 번째 독립 작업에서 반복 가치가 확인되기 전 기능을 확장하지 않는다.
4. 동일 실패가 반복되지 않으면 새 rule·skill을 추가하지 않는다.
5. 다음 계획 문서는 목표·변경·검증·성공 gate·commit 경계만 남겨 이번 547줄 계획보다 압축한다.
